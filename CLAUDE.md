# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/025-lifecycle-harness/plan.md](specs/025-lifecycle-harness/plan.md) — SDD-25 lifecycle integration harness (explicit AC-10 owner). Build `tests/integration/` build-tagged `//go:build integration` suite that drives the REAL `internal/supervise.Lifecycle` (SDD-24, already merged) plus REAL `internal/{server,audit,token,vault,transport/{ecies,sign},keys,cli}` packages in-process through the **17 named scenario test functions** locked in spec FR-002 (15 scenarios + Scenario 9 strict/grace split + Scenario 11 ready/boot-timeout split): `Test_Scenario_01_InteractiveShellRequest`, `…02_FirstDaemonBootstrap`, `…03_CleanChildExitRefill`, `…04_ChildCrashRefill`, `…05_ChildExit78Stale`, `…06_ValidatorFailure`, `…07_VaultRestartInvalidatesSession`, `…08_DaytimeRefresh`, `…09_OvernightExpiry_Strict`, `…09_OvernightExpiry_Grace`, `…10_DiscordUnavailable`, `…11_TailscaleReady`, `…11_BootTimeout`, `…12_AgentStatusCheck`, `…13_MidSessionRotation`, `…14_DuplicateStart`, `…15_LogPatternMatch`. **Only 4 boundaries mocked**: Discord (`testutil.DiscordStub` + per-scenario approve/deny/timeout/unavailable wrapper), 5 provider validator HTTP endpoints (loopback `httptest.Server` per provider, programmable 200/401/network-failure), wall clock (FakeClock backing `Deps.Clock` + `Deps.NowFn`), Tailscale probe (`Deps.TailscaleProbe` stub for Scenario 11). Nothing else mocked. **Each scenario asserts 4 contracts** (locked in `contracts/scenario-assertions.md`): (A) final supervisor / interactive state matches LIFECYCLE-SCENARIOS.md; (B) audit JSONL contains documented events in documented relative order (intervening tolerated) + `audit.Verify` hash-chain continuity; (C) supervisor scenarios assert status-socket JSON conforms to SPEC.md §FR-12; (D) `AssertSentinelAbsent` over 6 captured byte streams (operational slog, audit JSONL, status-socket response, Discord alert payloads, child stdout+stderr, scenario error strings). **Harness file inventory (locked at 6 files in chunk doc — research.md §1 affirmed)**: `tests/integration/harness/{vault,server,discord,supervisor,child,log_capture}.go` + scenarios live together in `tests/integration/scenarios_test.go` + suite-wide setup in `tests/integration/lifecycle_test.go` (including the `TestMain` that recognises `os.Executable()` re-invocation in integration-child-mode per research.md §6). All files `//go:build integration`. **Suite gates**: 17/17 green under `-race`; < 120 s wall-clock on developer-class machine; 5 consecutive runs flake-free; zero `t.Parallel` at scenario level (spec FR-022); zero `t.Skip` (spec FR-001); zero `time.Sleep` to drive documented transitions (use bounded `runtime.Gosched()` poll + injected clock); zero outbound network egress (spec FR-012 + SC-004 — `http.Client.Transport` is a `RoundTripper` rejecting any host outside 127.0.0.1/::1/registered httptest endpoints). **AC-10 owner-of-record**: implement-phase appends the 17 test names to `docs/AC-MATRIX.md` AC-10 row, updates AC-9 row (test-infra completeness), appends a `tests/integration/` entry to `docs/PACKAGE-MAP.md` listing the harness purpose WITHOUT freezing harness type signatures (they evolve as new chunks add scenarios — per SDD-25 chunk doc), and marks SDD-25 status `done` in `docs/SDD-PLAYBOOK.md`. **Constitution scope**: **II** (Discord stub is programmable per-scenario — never an auto-approve path; adapter contains zero policy), **V** (data-model.md §2 documents every alert emission; Contract B enforces `supervisor_stale_alert` audit row per alert-emitting scenario; Scenario 15 asserts alert WITHOUT state change), **VIII** (TDD-mandatory; spec FR-001/008/009/010/011; all 17 names locked in FR-002), **IX** (every harness goroutine has `t.Cleanup`-registered termination; `TestSupervisor.AssertNoGoroutineLeak` uses pre/post `runtime.NumGoroutine` snapshot with bounded `runtime.Gosched()` drain; zero `init()` — `TestMain` is testing-framework hook, not Go init; zero package-level mutable state in harness; consumer-side single-method interfaces `Validator`/`Alerts`/`Watchdog` already in SDD-24 — no new ones; zero `reflect`), **X** (6-stream sentinel-absence sweep at scenario end; fixture vault built from `testutil.SentinelSecret(N)` plaintext; audit JSONL scanned as raw bytes BEFORE JSON-aware processing; ZERO new `string(*SecureBytes)` sites introduced by this chunk — the two existing Constitution-X-permitted sites in SDD-24 remain the only ones), **XI** (zero new direct `go.mod` deps — stdlib + already-in-module helpers only). **Anti-contracts**: NEVER `t.Parallel` at scenario level (FR-022); NEVER `t.Skip` (FR-001); NEVER `time.Sleep` to drive documented transitions; NEVER hit non-loopback hostnames; NEVER add a new exported method on any locked SDD-19..24 type (research.md §R7 affirms clock seam uses `Deps.Clock` + direct refill-callback invocation, NOT package-private setter access); NEVER add a `//go:build integration` file inside any production package; NEVER `string(*SecureBytes)` anywhere; NEVER add an `init()`; NEVER add `harness.Skip`/`harness.Sleep`/`harness.Reset`/`harness.Suppress*` (locked off in `contracts/harness-api.md` anti-API); NEVER rename a row in spec FR-002's locked 17-name list without amending the spec first. **Complexity Tracking is EMPTY** — no Constitution Check violations; every gate passes by construction. Primary AC: AC-10 (15-scenario lifecycle deliverable, the v0.1.0 release gate).
<!-- SPECKIT END -->
