# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/027-watchdog/plan.md](specs/027-watchdog/plan.md) — SDD-27 log-pattern watchdog (alert-only) for the AC-10 supervisor lifecycle subset. New sub-package `internal/supervise/watchdog` (R-001 avoids the SDD-24 `supervise.Watchdog` interface name collision) with one file `watchdog.go` exporting **3 types + 3 chunk-doc-named methods + 1 adapter method + 7 sentinel errors**: `type Pattern struct{Name string; Regex *regexp.Regexp; RateLimit time.Duration}`, `type Event struct{Pattern string; Line string; Time time.Time}`, opaque `type Watchdog struct{...}`, `NewWatchdog(patterns,alerts,logger) (*Watchdog, error)` (R-002 extends chunk-doc bare return per FR-007a Clarification Q5: rejects duplicate pattern names with `ErrDuplicatePatternName`), `(*Watchdog).Ingest(line []byte)` (non-blocking, defensive copy, queue-full drops episode-coalesced per Q4), `(*Watchdog).Run(ctx) error` (single-shot via `ran atomic.Bool`, spawns ONE matcher goroutine, drops pending lines on `<-ctx.Done()` and INFO-logs count per R-007, returns within SC-004's 250ms), `(*Watchdog).OnStderrLine(ctx, line)` (R-003 adapter satisfying `supervise.Watchdog` interface for `Deps.Watchdog` wiring — additive beyond chunk-doc API), and seven sentinels `ErrAlreadyRan / ErrEmptyPatternName / ErrDuplicatePatternName / ErrNilPatternRegex / ErrNonPositiveRateLimit / ErrNilAlertsChannel / ErrNilLogger`. **Internals (R-004..R-016)**: clock seam `now func() time.Time` (unexported; default `time.Now`); internal `lines chan []byte` capacity 512 (R-005); per-pattern `bucketState{tokens, lastRefill, suppressedCount}` token bucket capacity 1 lazy-refilled (R-006, FR-004 default 600s = 3600/6); `dropEpisode{count, firstDropAt}` for FR-010a queue-full episode coalescing (R-008); `enqueueMu sync.Mutex` guards Ingest+drop bookkeeping (R-009); `cancelled atomic.Bool` short-circuits post-Run Ingest (R-009); WARN per match for rate-limit (FR-005/006) AND per drop for alert-output saturation (R-010), one WARN per episode for queue-full (R-008), one INFO with drop count on cancel; alerts channel NEVER closed (R-011, SDD-28 multi-producer model). **24 mandatory tests** in `watchdog_test.go` (TDD-first per Constitution VIII), names locked in quickstart.md §4 covering B-W-1..B-W-25 black-box contracts. Coverage target ≥90% via `go test -race -cover ./internal/supervise/watchdog/`. **Scope**: Lifecycle Scenario 15 emission half (`[STALE] Log Pattern Match` routing belongs to SDD-28; audit row write belongs to SDD-28 per Clarification Q1). **Constitution scope**: **V** (every drop emits WARN, zero silent suppression; alert-only — FR-003 + B-W-17 `TestWatchdog_NeverTransitionsState` proves zero state-machine authority; package source MUST NOT name `Store`/`Refiller`/`Refresher`/`Grace`/`Lifecycle`); **VIII** (TDD-first, 24 named tests BEFORE implementation, race-clean, ≥90% coverage); **IX** (single matcher goroutine with explicit owner/ctx/termination; ctx-first `Run(ctx)`; sentinel errors via `var Err... = errors.New(...)` exempt as Constitution-IX-sentinel-class globals per SDD-21 precedent; zero `init()`; zero mutable package-level state; one unexported `const lineChannelCapacity = 512` is a compile-time constant; defer-recover at matcher top frame); **X** (NO `internal/vault/securebytes` import, NO `*SecureBytes` field, single `string(...)` site is `string(line)` for non-secret Event.Line construction — child stderr is operator log content, NOT vault material; all three WARN emission sites EXCLUDE matched-line content per Clarification Q2; sentinel-byte assertions in B-W-8/B-W-12/B-W-13 prove it); **XI** (zero new direct `go.mod` deps — only `context`/`errors`/`fmt`/`log/slog`/`regexp`/`sync`/`sync/atomic`/`time` stdlib + the `supervise.Watchdog` interface guard reference). **Anti-contracts**: NEVER trigger any state transition, child-restart, child-signal, session-claim, session-refresh, or session-revocation (FR-003); NEVER drop alerts silently (FR-005/006/011, FR-010a); NEVER include matched-line content in WARN entries (Q2); NEVER close the alerts channel (R-011); NEVER re-compile patterns inside Ingest/Run (FR-008, SC-006 — caller pre-compiles via `regexp.Compile`, watchdog reuses); NEVER spawn a goroutine outside Run; NEVER allow Run to be called twice (R-012); NEVER block Ingest (FR-010a). **Complexity Tracking**: 3 justified extensions (sub-package location vs chunk-doc `internal/supervise`; `NewWatchdog` error return; additive `OnStderrLine` adapter) — each rationalized in plan.md §Complexity Tracking with rejected alternatives. **Files**: `internal/supervise/watchdog/watchdog.go` + `internal/supervise/watchdog/watchdog_test.go` (sub-package; mirrors SDD-18 `internal/supervise/config` precedent). Primary AC: AC-10 Scenario 15 (alert-only watchdog).
<!-- SPECKIT END -->
