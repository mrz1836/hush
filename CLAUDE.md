# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/023-cli-supervise-and-client/plan.md](specs/023-cli-supervise-and-client/plan.md) — SDD-23 `hush supervise` + `hush client status` + `hush client refresh`. **Pure orchestration glue** over SDD-18 (config), SDD-19 (state.Store), SDD-20 (Child), SDD-21 (Refiller/Refresher/Grace), SDD-22 (PidFile/StatusServer). Two new files in `internal/cli/`: `supervise.go` (~350 LOC, foreground orchestrator subcommand parsing `<config-path>`, `--dry-run`, `--grace-window <dur>`, `--no-cache`; on dry-run builds canonical `/claim` payload via `internal/transport/sign.CanonicalJSON` and exits 0 with NO Discord/vault contact per FR-023-9; on normal start sequences `AcquirePidFile` → `NewStore` → `NewGrace(effectiveTTL, effectiveEnabled)` → `NewRefiller+attach` → `NewRefresher` → `NewStatusServer+attach(inputs)+attachRefreshHandler(coalescer.Handle)` → `wg.Add(2)` for StatusServer+Refresher goroutines → initial `Refill` → `NewChild+Start` → child-exit wait loop dispatching `EventChildExit78Stale`/`EventChildExitClean`/`EventChildExitCrash` to SDD-19 with `ErrJTIUnknown` → `EventFetchAuthRequired` → grace-cache fallback when enabled → respawn; on SIGTERM/SIGINT via `signal.NotifyContext` cancels rootCtx → `child.Forward(SIGTERM)` → `child.Wait` → `wg.Wait` → defer `pidfile.Release`; flag overrides applied pre-side-effect: `--grace-window` validated `>0 && ≤4h` else `errInvalidGraceWindow→ExitInputErr`, `--no-cache` beats `--grace-window` per FR-023-14) and `client.go` (~250 LOC, `client status` + `client refresh` leaf subcommands; socket-path precedence per FR-023-15 = `--socket` > `--supervisor NAME` via new `supervise.SocketPathForSupervisor` > auto-detect via new `supervise.EnumerateSupervisorSockets`; status: 2s ctx.WithTimeout → DialContext unix → write `status\n` → read; TTY-detect via `term.IsTerminal(stdout.Fd())` else pipe; `--json` flag bypasses TTY; JSON path writes raw socket bytes verbatim (no re-marshal) to preserve schema authority; refresh: 90s ctx.WithTimeout → write `refresh\n` → unmarshal `{ok,error}` ack; `{"ok":true}` → ExitOK, `{"ok":false,"error":<msg>}` → `errSupervisorRefused→ExitErr` with msg on stderr; no `--json` on refresh). **One minimal extension to `internal/supervise/socket.go`** (Complexity Tracking row 1): verb dispatch on first request line → `status` (default, includes empty/unrecognised — backward-compat) vs `refresh` (route to `refreshHandler func(ctx)error` callback wired via new package-private `(*StatusServer).attachRefreshHandler` mirroring `attach(StatusInputs)` precedent); handler returns nil → `{"ok":true}\n`, non-nil → `{"ok":false,"error":<one-line msg>}\n` with `\n` in error replaced by spaces; unwired handler returns stable `refresh handler not wired` error (no panic). Two new production helpers in `socket_{darwin,linux}.go` (NOT test-fixture-only like `defaultRuntimeDir`): `SocketPathForSupervisor(name string) string` (Darwin: `<UserCacheDir>/hush/supervise-<name>.sock`; Linux: `<XDG_RUNTIME_DIR>/hush-supervise-<name>.sock`) + `EnumerateSupervisorSockets() ([]string, error)` (sorted matches of platform naming scheme; missing dir → empty slice, not error). Refresh coalescing per FR-023-22a implemented in `internal/cli/supervise.go` via private `refreshCoalescer{mu sync.Mutex; inflight *refreshFlight; perform func(ctx)error}` single-flight (NO `golang.org/x/sync/singleflight` import — Constitution XI bars new direct dep); concurrent refresh requests share one terminal ack from the in-flight refill. Orchestrator-private `orchestratorInputs` struct implements `supervise.StatusInputs` via 8 atomic.Pointer/Bool fields; private `claimPreview{name,reason,scope,session_type,requested_ttl,machine_index}` struct fed to `sign.CanonicalJSON` for dry-run preview (NEVER `sign.Sign` on dry-run path). Exit-code mapping additions in `exit_codes.go`: `errInvalidGraceWindow→ExitInputErr`, `errSocketAmbiguous→ExitInputErr`, `errSocketUnreachable→ExitErr`, `errSupervisorRefused→ExitErr`, `errDuplicateSupervisor→ExitErr` (wraps `supervise.ErrPidLocked` with FR-023-6 message naming pidfile path); `ExitConfigStale=78` reserved (raised only by supervise when surfacing child's verbatim exit-78 to service manager AND `restart_on_exit_78=false`). Coverage target ≥85% on `supervise.go`+`client.go` (SC-023-10); SDD-22 socket.go coverage MUST remain ≥95%. Test surface mandated by Constitution VIII TDD: ~12 unit tests in `supervise_test.go` (including grep-based `TestSupervise_OrchestrationDelegatesToInternalSupervise` asserting no `case StateRunning`/`switch state`/raw `78` exit-code arithmetic/`runtime.GOOS`/HTTP/TCP/bearer substrings), ~14 unit tests in `client_test.go` (including 3 auto-detect ambiguity cases + sentinel-leak), 1 integration test `TestSuperviseIntegration_DryRunWithDiscordStub` (full dry-run + DiscordStub round-trip asserting no Discord call, no pidfile, no socket binding), plus ~10 new socket-test cases in `socket_test.go` for verb dispatch + path helpers. **Anti-contracts**: NEVER `runtime.GOOS` or per-OS branch in `internal/cli/supervise.go`/`client.go` (Constitution VII; grep test enforces); NEVER `case StateRunning`/`switch state`/raw state-string literals in `supervise.go` (state-table reasoning owned by SDD-19; grep enforces); NEVER hard-coded `os.Exit(N)` or numeric exit-code arithmetic — all exit codes via `mapErr` (SDD-14 convention); NEVER `net.Listen("tcp",...)`/`net.Dial("tcp",...)`/`http.Server`/`http.ListenAndServe`/`"Bearer "` literal (Constitution V); NEVER `string(decryptedBytes)` or any `string(*securebytes.SecureBytes)` conversion (Constitution X); NEVER `init()` or new package-level mutable `var` (Constitution IX); NEVER spawn goroutine without owner+ctx+termination+top-frame `recover` (Constitution IX — orchestrator owns exactly 2 spawned goroutines: StatusServer+Refresher); NEVER `os.Exit` inside any subcommand RunE (return error, `mapErr` resolves); NEVER `sign.Sign(...)` on `--dry-run` path (FR-023-9 — render canonical bytes only, no signing keychain access); NEVER contact vault server on `--dry-run` (FR-023-9); NEVER add new `go.mod` direct dependency (Constitution XI — `cobra`/`slog`/`golang.org/x/term`/`sign` already in module); NEVER add new exported package-level symbols in `internal/cli` (cobra command tree IS the contract — SDD-14/15/16/17 lock); NEVER alter any SDD-18/SDD-19/SDD-20/SDD-21 symbol (PACKAGE-MAP locks); NEVER `omitempty` FR-12 fields on the status JSON path (defer to SDD-22's locked DTO — client `--json` writes raw bytes verbatim); NEVER retry `Dial`/`Read`/`Write` internally on either client subcommand — single attempt within the bounded ctx (FR-023-19 2s status, FR-023-24 90s refresh); NEVER print supervisor's refresh-error message to stdout (stderr only per FR-023-22); NEVER include token/secret/JWT bytes in any error message, slog record, or socket response — error messages identify failure mode + non-secret identifier (scope name, supervisor name, socket path) only (FR-023-27/28, Constitution X); NEVER add `--human` flag (FR-023-17a symmetric flag rejected); NEVER add `--json` flag on `client refresh` (FR-023-17a — refresh has no format flag); NEVER expose a "busy" exit class to refresh client (FR-023-22a — coalescing is invisible to client, both callers share terminal ack); NEVER add HTTP-over-Unix-socket dispatch with URL paths (Complexity Tracking row 1 alternative — verb match is a single-line trim+compare, not URL parsing); NEVER panic on unwired refreshHandler in socket.go (write stable error response); NEVER backwards-incompatible the existing status path (empty/unrecognised verb continues to return status doc verbatim per SDD-22 §2.5 advisory-payload). Constitution scope: **IV** (TTL discipline applied via SDD-18's cap surfaced by `mapErr`; supervisor zeroes secrets after handoff via SDD-21 Grace; `--no-cache` strict-mode override), **V** (Unix socket FS perms = auth, no HTTP/TCP/bearer added; status-visible operator-facing freshness API via `client status`+`client refresh`; child exit-78 propagated through SDD-19 state table), **VII** (cobra-only, noun-verb pattern `hush supervise <path>` + `hush client {status,refresh}`, global flags inherited from SDD-14 root, TTY/JSON convention NFR-10, exit codes 0/1/2/3/4/5/78 mapped via `mapErr`), **VIII** (TDD-mandatory ≥85% coverage; grep-based delegation-enforcement test; sentinel-leak tests via `test_sentinels_test.go` pattern; AC-10 Scenarios 12 + 14 + 13 covered end-to-end), **IX** (sentinel `var Err… = errors.New(…)`; `%w` wrapping; no init; no globals; consumer-defined `StatusInputs` already; 2-goroutine inventory each with owner+ctx+termination+recover; signal-derived `signal.NotifyContext` is sole new ctx derivation; `defer pidfile.Release()` AFTER `wg.Wait()`), **X** (no `string(secret bytes)`; token bytes never reach wire — SDD-22 DTO already enforces; error messages carry mode/identifier only; sentinel-leak test asserts no marker bytes escape stdout/stderr/logs), **XI** (no new `go.mod` direct dep; single-flight is a 6-line mutex pattern not `singleflight`). Primary AC: AC-10 Scenarios 12 (`client status` freshness gate) + 13 (`client refresh` triggers mid-session rotation completion) + 14 (duplicate `hush supervise` refused via `ErrPidLocked` → `errDuplicateSupervisor`).
<!-- SPECKIT END -->
