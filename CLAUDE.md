# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/020-supervise-child/plan.md](specs/020-supervise-child/plan.md) — SDD-20 `internal/supervise` child fork/exec + signal forwarding + exit-78 + pgid death-watch. **Extends `package supervise`** (alongside the SDD-18 `config/` subpackage and SDD-19 `state.go`) with three behaviour files: `child.go` (cross-platform `Child` + `ChildConfig` + `NewChild` + `Start` + `Wait` + `Forward` + `PID` + sentinel errors `ErrChildNotStarted`/`ErrCommandEmpty`/`ErrCommandPathRelative` + `const Exit78 = 78` + `*ringBuffer` for 64 KB FIFO-evicting bounded pipes), `child_linux.go` (//go:build linux — `SysProcAttr.Pdeathsig = SIGTERM` for kernel-enforced parent-death cleanup; `startDeathWatch` no-op), `child_darwin.go` (//go:build darwin — kqueue/EVFILT_PROC death-watch goroutine pair watching PPID via self-pipe wake; **best-effort per R-009**: SIGKILL-of-supervisor gap documented). Three test files: `child_test.go` (T-01..T-09, T-12, T-13 cross-platform), `child_linux_test.go` (T-11a Pdeathsig), `child_darwin_test.go` (T-11b kqueue graceful path; SIGKILL subtest skips R-009). Path validation **at Start** (not `NewChild`): `len(cmd)==0` → `ErrCommandEmpty`; `!filepath.IsAbs(cmd[0])` → `ErrCommandPathRelative` (FR-020-2/3 — distinct sentinels per Clarification implicit in spec; chunk doc's locked sentinel list **expanded by one** to honour FR-020-3 distinctness, see R-003). NEVER prepend `/bin/sh -c`; NEVER call `LookPath`. `cmd.SysProcAttr.Setpgid = true` cross-platform (FR-020-4). Exit disposition derived from `cmd.ProcessState.Sys().(syscall.WaitStatus)`: signaled → `(0, sig, nil)`; status-coded → `(code, 0, nil)`; verbatim — Exit78 NEVER coerced (FR-020-9/10, R-007). Concurrency: `sync.Once` guards `cmd.Wait()` body; **first caller wins** the exit triple, **all concurrent + subsequent callers** observe cleared `c.cmd` and return `(0, 0, ErrChildNotStarted)` per Clarification 1 (R-004). Single sentinel `ErrChildNotStarted` covers every "no live child" case — never started, post-Wait re-entry, signal forwarded after child exit (per Clarification 2, R-011); NEVER add `ErrChildExited`. Forwarding goroutine: `select { ctx.Done() | childDone | forwardCh }` per Clarification 3 (R-013); `syscall.Kill(-pgid, sig)` to deliver to PGID. Drain goroutine **per stream** (R-014): blocks on slow sink indefinitely (Clarification 4) without back-propagating to daemon — bounded ring buffer absorbs floods via FIFO eviction; `*ringBuffer.Write` ALWAYS returns `(len(p), nil)` (FR-020-13). Overflow warning: **one `slog.Warn` per episode per stream** via `atCapacity` flag flip (Clarification 5, R-006); episode = continuous period at capacity; resets when buffer drains below cap. Goroutine roster per `Start`: linux=3 (forward + 2 drain), darwin=5 (forward + 2 drain + death-watch + self-pipe-waker); **all joined via `c.wg.Wait()`** inside `Wait`'s `Once.Do` body. After winning Wait: cached `*exec.Cmd` cleared under write lock (FR-020-11 — no caching released resources, Constitution IX); subsequent `Wait`/`Forward`/`PID` observe `cmd == nil`. Restart cycles: 100× sequential `NewChild`+`Start`+`Wait` returns `runtime.NumGoroutine()` to baseline (SC-020-6, T-09b). Test-helper protocol: `os.Executable()` re-invocation gated by `HUSH_CHILD_TEST_HELPER_MODE` env-var with `TestMain` dispatch (R-012); NEVER add a `cmd/test-helper-*` binary. **No fuzz target** at this layer (R-008) — commands are typed `[]string` from validated SDD-18 config; signals are typed `os.Signal`; no parser surface. **Anti-contracts**: NEVER prepend shell or call `LookPath` (FR-020-1); NEVER add `Restart()` — `Child` is single-use; NEVER add `ErrChildExited` (Clarification 2); NEVER make `Wait` ctx-cancellable — flow cancellation through `Forward(SIGTERM)` (R-013); NEVER allow `cfg.Logger == nil` — panic at `NewChild` (Constitution IX startup-wiring exemption); NEVER spawn forwarding/drain/death-watch goroutines outside `Start`; NEVER alter any SDD-18 (`config/`) or SDD-19 (`state.go`) symbol; NEVER perform any I/O outside the child boundary — no network, no vault, no Discord, no audit, no SDD-19 `Transition` (FR-020-16, wiring is SDD-21's job); NEVER claim Darwin handles SIGKILL-of-supervisor — R-009 gap surfaced via `t.Skip("R-009 darwin gap")`; NEVER introduce `internal/testutil` helpers — fakes inline (R-015). Coverage target ≥90% on `internal/supervise/{child, child_linux, child_darwin}.go` (SC-020-8, Constitution VIII High band). Constitution scope: IV (lifecycle integrity — child exit never reaches `stopped`; supervisor decides via SDD-21), VIII (TDD-mandatory; 14 tests T-01..T-13 authored before implementation in 7 waves; race-clean), IX (`os/exec` stdlib; no shell; no `init()`; sentinel-class `var Err…` and `const Exit78`; every goroutine has explicit termination + top-frame `recover`; single-method interfaces consumed via `*slog.Logger`), X (no secret values in errors; overflow `slog.Warn` carries stream label only — never buffer contents). AC-10 primary. **Zero new direct dependencies** (`golang.org/x/sys/unix` already a direct dep at v0.43.0 for darwin kqueue). Tests use `os.Executable()` re-invocation + table-driven harnesses; no new external test helpers.
<!-- SPECKIT END -->
