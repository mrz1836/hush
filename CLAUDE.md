# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/030-examples-and-tailscale/plan.md](specs/030-examples-and-tailscale/plan.md) — SDD-30 generic operator-facing supervisor TOML + verify-and-polish two pre-existing operator-agnostic docs. **Scope:** one NEW file `deploy/examples/supervisors/example-daemon.toml` (fully commented, fully generic; every CONFIG-SCHEMA.md §Supervisor-config field appears as an active example value with an inline `# <one-sentence purpose>. Required.` or `# <purpose>. Default: <loader-default>.` comment; top-of-file block links to `docs/CONFIG-SCHEMA.md#supervisor-config` + `docs/TAILSCALE-ACLS.md` + `docs/CLEAN-MACHINE.md` + the AC-6 Keychain-ACL contract); two doc files patch-edited (`docs/TAILSCALE-ACLS.md` + `docs/CLEAN-MACHINE.md`); one NEW test file `internal/supervise/config/example_test.go` containing `TestExamples_GenericTOMLValidates` (loader round-trip; `TestExamples_NoOperatorSpecificNames` added in /speckit-tasks per FR-007). **Locked HOW (plan research.md):** (R-001) **Schema authority = SDD-18 loader + CONFIG-SCHEMA.md, NOT the prompt's "locked HOW" snippet** — the prompt's snippet (`[grace] window`, `dm_rate_limit`, `[child].env`, `[validators.X].type` sub-table, `[discord].audit_channel_id`, `[watchdog].patterns=[{name=,regex=,rate_limit=}]`) is rejected by `DisallowUnknownFields` and would break FR-005; spec §Assumptions names CONFIG-SCHEMA.md as source of truth. Template mirrors `internal/supervise/config/testdata/valid_maximal.toml` shape exactly, adds inline comments + top-of-file block + placeholders. (R-002) **TAILSCALE-ACLS.md patch:** the constitution VI + SECURITY.md §2.3 use `tag:trusted → tag:sandbox:7743`, but TAILSCALE-ACLS.md uses `tag:hush-agent → tag:hush-vault:7743`; patch realigns TAILSCALE-ACLS.md to present the constitutional pair as primary with the descriptive alternative noted as operator-valid; constitution NOT edited (out of scope). (R-003) **CLEAN-MACHINE.md §8 patch:** install.sh banner names the Keychain entry `hush-vault-passphrase`, but CLEAN-MACHINE.md §8 references only "hush-managed entries"; patch enumerates the three canonical entries (`hush-vault-passphrase` vault-host, `hush-discord` vault-host, `hush-client` agent-host) matching SDD-29 install.sh banner verbatim; installer NOT edited (out of scope per FR-010). (R-004) **`server_url = "http://100.64.0.1:7743/h/example"`** — first usable CGNAT address in `100.64.0.0/10`, canonical hush port, placeholder prefix; http (not https) per v0.1.0 (TLS-within-Tailscale is OUT-OF-SCOPE per Principle III Layer 6); loader's `validateServerURL` accepts as-shipped. (R-005) **Test path traversal:** `internal/supervise/config/example_test.go` → `../../../deploy/examples/supervisors/example-daemon.toml` (three `..` levels: config → supervise → internal → repo-root). Standard test package, no build tag. (R-006) **Two comment grammars:** Required → `# <one-sentence purpose>. Required.`; Optional → `# <purpose>. Default: <loader-default>.`; single CONFIG-SCHEMA.md anchor lives only in top-of-file block (clarification 5). **Field census (27 documented fields):** every CONFIG-SCHEMA.md §Supervisor-config field appears with literal value matching loader's default catalog, EXCEPT `audit_log` (deliberately omitted — loader auto-defaults to `<dirname(pid_file)>/<name>-audit.jsonl`; including it would force operator to invent a path; `pid_file` inline comment documents the auto-default). **Placeholder taxonomy (FR-004):** three categories — (a) human-readable slugs `example-daemon`, `your-daemon-binary`, `Example Daemon`, (b) scoped secret names `EXAMPLE_API_KEY_1` / `EXAMPLE_API_KEY_2`, (c) `REPLACE_ME` marker only for `[discord].alert_channel_id` (any literal snowflake would look real). **Constitution scope:** **I** (operator-agnostic — `TestExamples_NoOperatorSpecificNames` seed list in test source per FR-007/clarification 1; the three files delivered/touched by this chunk grep clean against the forbidden list); **VI** (Tailscale-only — `server_url` is concrete CGNAT inside `100.64.0.0/10` per FR-008; TAILSCALE-ACLS.md realignment per R-002 keeps perimeter doc consistent with constitution-VI). **VIII / IX / X / XI** are lightweight-pass (one new test file, no new dependencies, no new globals, template carries zero secret values — only non-secret labels). **Anti-contracts:** NEVER ship the prompt-snippet field forms (would break FR-005); NEVER edit constitution to match TAILSCALE-ACLS.md tags (out of scope); NEVER edit install.sh to match CLEAN-MACHINE.md naming (FR-010 forbids); NEVER include `audit_log` literal (forces operator-invented path); NEVER list `EXAMPLE_API_KEY_*` in `[child].env_passthrough` (env_passthrough is for non-secret env only — secrets flow through scope+JWT+ECIES); NEVER commit an operator-specific identifier into any of the three delivered/touched files (FR-007/FR-011 — `TestExamples_NoOperatorSpecificNames` grep enforces). **Complexity Tracking:** zero rows — Constitution Check passes clean both before and after Phase 1. **Files:** `deploy/examples/supervisors/example-daemon.toml` + `internal/supervise/config/example_test.go` (NEW); `docs/TAILSCALE-ACLS.md` + `docs/CLEAN-MACHINE.md` (EDITED — patch-level per R-002/R-003); `docs/PACKAGE-MAP.md` + `docs/AC-MATRIX.md` + `docs/SDD-PLAYBOOK.md` (EDITED in /speckit-implement step 6-8); `CLAUDE.md` (EDITED — this marker block). Primary AC: AC-6 (Keychain ACL — template comment block + CLEAN-MACHINE.md §8 enumeration), AC-8 (server hardening — CGNAT `server_url` literal + TAILSCALE-ACLS.md alignment), AC-10 (supervisor lifecycle — template populates the full schema so 15 scenarios are exercisable from it).
<!-- SPECKIT END -->
