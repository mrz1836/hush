# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/014-cli-root-and-server-cmds/plan.md](specs/014-cli-root-and-server-cmds/plan.md) — SDD-14 `cmd/hush/main.go` + `internal/cli` root + four operator subcommands (`serve`, `health`, `version`, `revoke`). Cobra-only (Constitution VII; **no viper**), four global persistent flags (`--config/-c`, `--verbose/-v`, `--quiet/-q`, `--no-color`), TTY-aware per-stream output (text on TTY, JSON on pipe; `--no-color` strips ANSI regardless), seven locked exit codes (`ExitOK=0, ExitErr=1, ExitInputErr=2, ExitAuth=3, ExitNotFound=4, ExitPerm=5, ExitConfigStale=78` — 78 is reserved-but-never-raised in this chunk for the future supervisor↔child contract). `serve`: passphrase resolution `stdin pipe → TTY prompt → ExitInputErr` (POSIX-line: strip exactly one trailing `\n` or `\r\n`, preserve all other bytes; **never** `os.Getenv`), then composes the chassis from already-locked surfaces (`config.LoadServer` → `keys.DeriveMasterSeed`/JWT/Vault/Audit subkeys → `vault.Load` → `audit.NewWriter` → `discord.NewBotApprover` → `server.New`+`RegisterHandlers`+`Run`); SIGTERM/SIGINT graceful via `signal.NotifyContext`; bot-token loaded from macOS `security`/Linux `secret-tool` (60-LOC inline helper). `health`: 5s-bounded `GET /hz`; locked literal-text connection-refused/timeout messages; partial-health surfaces full per-dimension summary AND `ExitErr` (FR-017a). `version`: build-injected `Version`/`Commit`/`Date` package vars; locked non-TTY JSON shape `{"version","commit","date"}` with `dev`/`unknown`/`unknown` placeholders. `revoke`: requires `--server`+`--jti`; signs `{jti,nonce,timestamp}` via `sign.CanonicalJSON`+`sign.Sign` (SDD-08); HTTP status → exit code map (200→0, 401/403→3, 404→4, 5xx/network→1). New deps: `github.com/spf13/cobra` (direct, prod) + `github.com/creack/pty` (test-only, sole consumer is no-echo passphrase test). Coverage target: 85% on `internal/cli`; passphrase-resolution + sentinel-leak paths reach 100%. Sentinel-leak tests use `SECRET_SHOULD_NEVER_APPEAR_14`. Locked exports: `cmd/hush.main`, `cli.Execute(ctx) int`, the seven `Exit*` constants. AC-1.
<!-- SPECKIT END -->
