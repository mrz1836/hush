# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/018-supervise-config/plan.md](specs/018-supervise-config/plan.md) — SDD-18 `internal/supervise/config` per-supervisor TOML schema + validation. NEW package; first chunk under `internal/supervise/`. Files: `config.go` (`Supervisor` + `Child` + `DiscordRouting` + `Watchdog` + `Validator string` + decoded wire-shape + materializer), `defaults.go` (typed `Default*` / `Max*` `var`s + unexported `validatorAllowList`), `validate.go` (rule engine), `errors.go` (sentinel catalogue), `paths.go` (`~`/`Abs` helpers — duplicates SDD-06's pattern; consolidation deferred), `doc.go`, `config_test.go`, `validate_test.go`, `config_fuzz_test.go`. Decoder: `pelletier/go-toml/v2` (already locked by SDD-06; **no new direct dep**) with `DisallowUnknownFields(true)`. Validator allow-list = exactly 5 names `{anthropic, anthropic-oauth, openai, google-ai, github}` — `ErrUnknownValidator` carries the **RHS validator name only**, NEVER the LHS secret-name (FR-014/FR-020/Constitution X). Grace-window cap = `MaxGraceWindow = 4 * time.Hour` → `ErrGraceWindowTooLong` (Constitution IV TTL discipline + Layer-6 audit boundary); contradiction guard: `cache_secrets_for_restart=false` (or absent) AND `cache_grace_ttl` explicitly set → `ErrGraceTTLWithoutCache` (Clarification 3; pointer-discriminator `*string` distinguishes absent vs explicit-zero). Refresh-window: split on single `-`, parse each side via `time.Parse("15:04", …)`; format violations → `ErrRefreshWindowFormat`; format-clean but `start >= end` (incl. wrap-around) → distinct `ErrRefreshWindowOrder` (FR-005/FR-006). `[child].command`: `[]string` non-empty AND `filepath.IsAbs(cmd[0])` — `ErrCommandEmpty` / `ErrCommandPathRelative` (distinct sentinels per FR-007); subsequent elements verbatim, no shell parsing. `requested_ttl`: parses as duration AND ≤ `MaxRequestedTTL = 24 * time.Hour` (the v0.1.0 absolute ceiling — Clarification 1; loader does NOT consult server config) → else `ErrRequestedTTLOutOfRange`. `server_url`: `url.Parse` + non-empty Host + scheme `http`/`https` (case-insensitive) → else `ErrServerURLInvalid`; deeper Tailscale-CIDR / port-7743 / `/h/<prefix>` checks deferred to runtime hardening (Clarification 5). Watchdog: missing `[watchdog]` ≡ all-fields-absent (Clarification 4) → `Enabled=true`, `Patterns=[]string{}` (non-nil), `MaxAlertsPerHour=6`; `MaxAlertsPerHour <= 0` → `ErrWatchdogRateInvalid`. Required-field gate runs FIRST (after decode, before format/range) and aggregates with `errors.Join` per dotted TOML path (`child.command`, etc.). Defaults applied AFTER decode for every absent optional field per FR-016 — every default in `docs/CONFIG-SCHEMA.md` Supervisor section asserted by a `TestSuperviseConfig_Default*` test. `Load(ctx, path)` is the ONLY entry point — single-shot, synchronous, idempotent, no goroutines, no `init()`, no env reads for secret-bearing fields (only `os.UserHomeDir` for `~` expansion of `status_socket`/`pid_file`; `$HOME` is non-secret per Constitution X). Public struct holds **zero secret values** (FR-014) — only non-secret labels: scoped secret NAMES, validator TYPE NAMES, env-var NAMES, file paths, Discord channel IDs, log patterns. `Validator string` typedef gives downstream a type-narrow signal that values are constrained; SC-005 asserts loaded config can never carry an out-of-allow-list value. Fuzz target `FuzzSuperviseTOML` (Constitution VIII fuzz #5; distinct from SDD-06's `FuzzServerTOML`) — random bytes → `Load` via `t.TempDir`-scoped file; assertions: no panic, no untyped error, every error matches a named sentinel via `errors.Is`. Seed corpus: 8 files (minimal-valid, full-default, malformed-bytes, empty, partial-table, conflicting-types, unknown-validator-name, refresh-window-edge). 60s gate. Coverage target ≥95% on `internal/supervise/config` (Constitution VIII High band). **Anti-contracts**: NEVER add an `init()`; NEVER consult environment variables for any supervisor field (FR-015); NEVER include the LHS secret-name (or any byte from a value) in `ErrUnknownValidator`'s wrapping message; NEVER read the server's `~/.hush/config.toml` to compare TTLs; NEVER enforce Tailscale-CIDR membership at load time (runtime hardening in SDD-19/SDD-23 owns it); NEVER auto-create the `status_socket` / `pid_file` parent dir (runtime startup owns it); NEVER spawn a goroutine; NEVER add a `LoadReader(io.Reader)` sibling API for fuzz convenience (locked contract is `Load(ctx, path)` only); NEVER alter any SDD-06 symbol. Constitution scope: IV (TTL + grace-window cap), V (operator visibility, validator allow-list explicit), VIII (TDD + 95% + fuzz #5), IX (no `init`, no globals beyond sentinel-class `var`s, no goroutines, errors wrapped with `%w`, modules-only, CGO-disabled), X (no secret values in struct or errors; type-driven redaction), XI (zero new direct deps — reuses pelletier/v2 from SDD-06). AC-10 primary. Validate gates pass on darwin AND linux. Tests use inline `t.TempDir`-scoped TOML literals + `testdata/` golden fixtures; no new `internal/testutil` helpers.
<!-- SPECKIT END -->
