# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/028-discord-alerts/plan.md](specs/028-discord-alerts/plan.md) — SDD-28 operator-facing Discord alert surface (8 classes + 3 tiers + per-supervisor/per-pattern debounce + commit-on-success transport) for AC-3 + AC-10 emission half. New sibling sub-package `internal/discord/alerts` (R-001 — PACKAGE-MAP.md:1244-1246 directs SDD-28 here and forbids altering SDD-11-locked `internal/discord` symbols; mirrors SDD-18 `config` + SDD-27 `watchdog` precedent) exporting **2 types + 11 constants + 2 structs + 1 interface + 2 funcs + 3 sentinels + 1 default const = 22 exported symbols** (data-model.md §1 + observable-behaviors.md): `type AlertClass string` + 8 constants (`AlertClassApprovalRequest / AlertClassDaemonRefreshRequest / AlertClassValidatorStaleFailure / AlertClassChildExit78StaleFailure / AlertClassLogPatternStaleWarning / AlertClassDiscordDisconnected / AlertClassDiscordReconnected / AlertClassVaultUnreachableAtBootTimeout` — names verbatim per `docs/LIFECYCLE-SCENARIOS.md` "Required alert classes" lines 301-314); `type Tier int` + 3 constants (`TierCritical=0 / TierWarning=1 / TierInfo=2`); `type Alert struct { Class, Tier, SupervisorName, MachineName, Pattern, Detail string; Time time.Time }`; opaque `type Router struct{...}`; `type Sender interface { SendOwnerDM(ctx, message) error; PostChannel(ctx, channelID, message) error }` (R-003 consumer-side interface replaces chunk-doc `discord.Approver` param — locked SDD-11 Approver only exposes RequestApproval and cannot be altered per PACKAGE-MAP.md:1245-1246; production `*BotApprover` gains additive `SendOwnerDM`/`PostChannel` methods in this chunk's implementation phase + compile-time `var _ alerts.Sender = (*discord.BotApprover)(nil)` guard in `internal/discord` — one-way import direction); `NewRouter(sender Sender, auditChannelID string, perSupervisorBucket, perPatternBucket time.Duration, logger *slog.Logger) *Router` (panics on nil sender / nil logger per Constitution IX startup-wiring exception; zero/negative duration → `DefaultBucketWindow=1*time.Minute`); `(*Router).Route(ctx context.Context, alert Alert) error`; three sentinels `ErrAlertRateLimited / ErrAlertTransport / ErrUnknownAlertClass` (last two are plan-time extensions per Clarification Q3 + FR-009). **Class→tier binding (R-002, locked immutable)**: ApprovalRequest=Critical, DaemonRefreshRequest=Critical, ValidatorStaleFailure=Warning, ChildExit78StaleFailure=Critical, LogPatternStaleWarning=Warning, DiscordDisconnected=Warning, DiscordReconnected=Info, VaultUnreachableAtBootTimeout=Critical — each row's tier traces to Constitution X "Discord alert tiers" / Constitution II "Approval is Phone" / Constitution V "Loud failure" with citation column in research.md R-002. Router IGNORES `Alert.Tier` and re-derives authoritative tier from `Alert.Class` via the immutable `classToTier` map (R-010; asserted by B-A-7 `TestRoute_CallerSuppliedTierIgnored`) — FR-004 forbids re-tiering on any runtime signal including caller-supplied tier. **Route() flow (R-007 single-shot, R-009 acquire/commit/refund)**: (1) class→tier lookup, unknown returns `ErrUnknownAlertClass` + WARN slog with `outcome=unknown_class`; (2) per-pattern key resolution — if `Alert.Pattern == ""` use `string(Class)` fallback (Clarification Q2 / FR-011a / R-005), else operator-supplied verbatim; (3) `supBucket.acquire(supervisorName)` — denied returns `ErrAlertRateLimited` (NO slog record per FR-016/Q5); (4) `patBucket.acquire(patternKey)` — denied refunds supBucket, returns `ErrAlertRateLimited`; (5) render template (omit-empty-lines per Q4/R-007: label-prefix + supervisor always; machine/pattern/detail only if non-empty; `strings.Builder`, no `text/template`, no placeholder text); (6) tier dispatch: Critical → `sender.SendOwnerDM(ctx, rendered)` single-shot zero-retry; Warning → `sender.PostChannel(ctx, auditChannelID, rendered)` single-shot; Info → no Sender call, slog INFO IS the delivery; (7) success → commit both buckets + DEBUG slog (Critical/Warning) or INFO (Info-tier already emitted at step 6); transport failure → refund both buckets + WARN slog + return `errors.Join(ErrAlertTransport, underlying)` (commit-on-success per Clarification Q3 / FR-012a / B-A-13+B-A-14). **Label prefixes (R-012)**: `[CRITICAL][approval-request]`, `[CRITICAL][daemon-refresh]`, `[WARNING][validator-stale]`, `[CRITICAL][child-exit-78]`, `[WARNING][log-pattern]`, `[WARNING][discord-disconnected]`, `[INFO][discord-reconnected]`, `[CRITICAL][vault-unreachable]` — 8 unique prefixes (asserted by B-A-16 `TestTemplate_LabelPrefixUniqueAndStable`); two-bracket `[TIER][class-slug]` shape mirrors SDD-11's `[STALE]`/`[DAEMON]` precedent. **Slog schema (R-008 / Clarification Q5 / FR-024a)**: exactly one slog record per Route call (zero on rate-limit path — caller logs per FR-016), message `"alert routed"`, level matrix `{Critical success=DEBUG, Warning success=DEBUG, Info success=INFO, ErrAlertTransport (any tier)=WARN, ErrUnknownAlertClass=WARN, ErrAlertRateLimited=NO RECORD}`; attribute allow-list `{class, tier, supervisor, machine, pattern, outcome}` — NEVER `detail` / `rendered` / `error` / `*http.Request` / credential-derived field (B-A-21 grep + recording-handler probe). **Rate-limit internals (R-009)**: two independent `*ratebucket` instances each with `sync.Mutex` + `map[string]bucketState{delivered, pending time.Time}` + injectable `now func() time.Time`; `acquire/commit/refund` honour FR-012a commit-on-success + FR-014 per-key isolation + FR-015 monotonic time (Go's `time.Now()` carries monotonic reading; `time.Sub` uses monotonic component regardless of NTP/DST/wall-clock jumps); mirrors SDD-11 `internal/discord/ratelimit.go` proven pattern. **25 mandatory tests** (TDD-first per Constitution VIII): 16 in `alerts_test.go` (B-A-1..7, 13..15, 19..22, 24, 25) + 3 in `templates_test.go` (B-A-16..18) + 6 in `ratelimit_test.go` (B-A-8..12, 23); names locked in observable-behaviors.md + quickstart.md §4 covering tier-routing (Critical-DM / Warning-channel / Info-no-Discord via `failOnInvokeSender`), tier-binding matrix, caller-supplied-tier-ignored, per-supervisor + per-pattern blocks-excess, per-key isolation, empty-Pattern class-fallback, applies-to-Info-tier, transport-failure-refunds-buckets (Critical + Warning), unknown-class-typed-error, label-prefix-unique, omit-empty-lines, no-secret-byte-leakage, zero-new-dependencies, slog level matrix, slog attribute allow-list, concurrent-safety (8 goroutines × 100 calls), monotonic-clock, sentinel-disjointness, NewRouter-nil-guards. Coverage target ≥90% via `go test -race -cover ./internal/discord/alerts/`. **Anti-coupling (R-016)**: alerts package imports ONLY stdlib (`context, errors, fmt, log/slog, strings, sync, time`); NEVER imports `github.com/bwmarrin/discordgo`, `internal/discord`, or `internal/vault/securebytes` (asserted by B-A-19 `TestRouter_ZeroNewDependencies`). **Scope**: Lifecycle Scenarios 2, 5, 6, 8, 10, 11, 15 emission half (the Discord-side operator surface — AC-3 alert-surface half + AC-10 alert-emission half); routing of `Event` from SDD-27 watchdog and `ErrStaleCredential` from SDD-26 validators flows into Router via SDD-24 lifecycle orchestrator wiring. **Constitution scope**: **V** (every transport failure / unknown-class emits WARN; class→tier binding code-asserted; no auto-promotion/demotion); **VIII** (TDD-first, 25 named tests BEFORE implementation, race-clean, ≥90% coverage, sentinel-byte test for FR-022/SC-009); **IX** (`Route(ctx, alert)` ctx-first; three sentinels via `var Err... = errors.New(...)` exempt as Constitution-IX-sentinel-class globals per SDD-21 precedent; `classToTier` + `classToTemplate` immutable package-level maps initialized by literal expression — same sentinel-class exemption; zero `init()`; zero mutable package-level state; ZERO goroutines spawned — Route is synchronous end-to-end on caller's goroutine; consumer-side `Sender` interface; `NewRouter` panics on nil sender/logger as startup-wiring exception); **X** (NO `*SecureBytes` import; rendered body formats ONLY `SupervisorName/MachineName/Pattern/Detail` allow-list; `Alert.Time` excluded from render body; log-attribute allow-list excludes `detail`; sentinel-byte test asserts no known-marker byte string survives any field into rendered output / errors / log records — Constitution X end-to-end). **Anti-contracts**: NEVER auto-promote Warning to Critical or vice versa (FR-004 — B-A-7); NEVER bypass rate-limit for any class including Info (FR-013 — B-A-12); NEVER format `Alert.Time` into rendered body (R-013 — B-A-18); NEVER reference any field outside `{SupervisorName, MachineName, Pattern, Detail}` in templates (FR-019/020 — B-A-18); NEVER consume debounce slot on transport failure (FR-012a — B-A-13+B-A-14); NEVER retry Discord call internally (FR-012b single-shot — B-A-13+B-A-14); NEVER alter SDD-11 locked surface (PACKAGE-MAP.md:1245-1246 — additive methods on `*BotApprover` are OK, alterations to `Approver` interface are forbidden); NEVER include `detail`/`rendered`/`error`/`*http.*` in slog attributes (FR-024 — B-A-21); NEVER emit slog record on `ErrAlertRateLimited` path (Q5/FR-016/FR-024a — B-A-8); NEVER import `discordgo`/`internal/discord`/`internal/vault/securebytes` from alerts package (R-016 — B-A-19); NEVER spawn a goroutine inside the package (Constitution IX — Route is synchronous); NEVER mutate `classToTier`/`classToTemplate` after declaration (A-4/A-5); NEVER add a 9th alert class or remove one of the 8 (FR-005 — chunk-level amendment). **Complexity Tracking**: 2 justified plan-time extensions to the chunk-doc-locked API (R-003: consumer-side `Sender` 2-method interface replaces `discord.Approver` parameter per Constitution IX + PACKAGE-MAP SDD-11 lock; R-011: three sentinels `ErrAlertRateLimited` + `ErrAlertTransport` + `ErrUnknownAlertClass` per Clarification Q3 + FR-009 defensive guard), each rationalised in plan.md §Complexity Tracking with rejected alternatives. Three structural choices need no extension marker (spec-documented behavior): `Alert.Tier` ignored at runtime — Router re-derives tier from `Alert.Class` per FR-004 + Spec Key Entities §Alert + R-010; `NewRouter` panics on nil Sender/logger per Constitution IX startup-wiring exception; zero/negative bucket windows fall back to `DefaultBucketWindow=1*time.Minute` per R-011 defensive default. **Files**: `internal/discord/alerts/alerts.go` + `templates.go` + `ratelimit.go` + `alerts_test.go` + `templates_test.go` + `ratelimit_test.go`. Primary AC: AC-3 (Discord-side operator surface, alert-surface half) + AC-10 (supervisor lifecycle alert emission half — Scenarios 2, 5, 6, 8, 10, 11, 15).
<!-- SPECKIT END -->
