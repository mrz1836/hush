# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/033-final-overhaul/plan.md](specs/033-final-overhaul/plan.md) — SDD-33 final repo + docs overhaul (drift reconciliation, dead-code sweep, README rewrite, drift-detection script). **Scope:** repo-wide audit-then-fix sweep across 11 categories A..K; produces a structured FINDINGS list at `specs/033-final-overhaul/findings.jsonl` per [contracts/audit-findings.md](specs/033-final-overhaul/contracts/audit-findings.md). **NEW artefacts:** `scripts/check-package-map-vs-code.sh` (drift detector — exit 0 against as-shipped tree per FR-013/SC-002, CI wiring deliberately deferred per Clarification 2026-05-15 Q2); `specs-archive/` directory receiving 32+ `git mv`-migrated spec subdirs per Clarification 2026-05-15 Q1 (FR-012); root `CONTRIBUTING.md` (NEW — documents the three policies per [research.md R-005](specs/033-final-overhaul/research.md)); extension to `internal/supervise/config/example_test.go` adding `TestExamples_NoOperatorSpecificNames_WholeTree` per [research.md R-004](specs/033-final-overhaul/research.md) (FR-014/SC-005 — zero matches required, exclusions = test fixture itself + specs-archive/ + .git/). **EDITED:** `README.md` (surgical edit per [research.md R-006](specs/033-final-overhaul/research.md) — FR-009/FR-010, quick-start added so a fresh reader can complete one round-trip without consulting another doc); `docs/PACKAGE-MAP.md` (FR-004 — appended-per-chunk → consolidated per-package per [research.md R-002](specs/033-final-overhaul/research.md), footer `*(Originally locked across SDD-NN, SDD-MM, ...)*` preserves attribution); `docs/AC-MATRIX.md` (FR-006 — every cited test path verified extant, renames updated); `docs/ARCHITECTURE.md` (FR-007 — diagram reconciled against `go list -f '{{.ImportPath}} {{.Imports}}' ./...`); `docs/IMPLEMENTATION-PLAN.md` (FR-011 — actuals subsection per phase noting deferrals/reorderings, especially SDD-24 mid-cycle activation); `docs/SDD-PLAYBOOK.md` (mark SDD-33 done at end of /speckit-implement; absorb stale SDD-25 narrative per pre-finding F-PRE-3); `docs/TESTING-STRATEGY.md` (FR-008 — re-verified; pre-finding F-PRE-1 confirms all 6 fuzz targets present). **Locked HOW (plan research.md, 8 R-NNN decisions):** (R-001) **Drift script algorithm** — `go doc -short -all ./internal/...` × per-package PACKAGE-MAP.md "Exported API" sections → normalised `<package> <symbol>` line sets → `diff`; non-empty diff = exit 1 with `+ doc-only` / `- code-only` lines; stdlib-only (Constitution XI). (R-002) **PACKAGE-MAP format** — one `## \`internal/<pkg>\`` section per package, sub-headings `Types`/`Functions`/`Constants`/`Variables`/`Sentinel errors` (omit empty), footer line preserves chunk attribution. (R-003) **specs/ migration** — `git mv specs/NNN-*/ specs-archive/` preserves history; in-flight `specs/033-final-overhaul/` stays put (SDD-32 migrates it post-merge). (R-004) **Operator-name whole-tree scan** — extend single seed list `operatorSpecificForbidden` in existing test file; `filepath.WalkDir` from repo-root; documented exclusions = test file itself + `specs-archive/` + `.git/`; standalone `scripts/check-no-operator-names.sh` is operator-ergonomics secondary, may slip to follow-on as minor finding. (R-005) **Root `CONTRIBUTING.md`** — NEW at repo root with three sections (specs archive policy, drift detection, operator-name allowlist); existing `.github/CONTRIBUTING.md` preserved as pointer (FR-019 forbids silent doc drop). (R-006) **README rewrite scope** — surgical edit, NOT from-scratch; quick-start section added (FR-010); badges + tech stack + doc table + architecture excerpt cross-checked against as-built; from-scratch path reserved for >15 inaccuracies. (R-007) **Rename criteria** — three-condition gate (PACKAGE-MAP drift demands it AND internal-to-internal AND announced as breaking change); `internal/cli` exit codes + `Execute(ctx) int` are off-limits (FR-016); failing the gate → document inconsistency in PACKAGE-MAP, leave code alone. (R-008) **Coverage discipline under dead-export removal** — security-critical packages (constitutional fenced block lines 253-261: keys/vault/securebytes/token/transport-sign/transport-ecies/audit) require pre-flight baseline + post-removal 100% recheck via `magex test:coverrace -run '^Test' ./internal/<pkg>/...`; ALL such removals are major findings (never minor). **Pre-findings already surfaced:** F-PRE-1 (six fuzz targets all present — `FuzzVaultDecode`/`FuzzJWTValidate`/`FuzzECIESDecrypt`/`FuzzVerifyRequest`/`FuzzServerTOML`/`FuzzSuperviseTOML`; bonus: `FuzzDeriveMaster`+`FuzzStatusJSON_Encode` are extras), F-PRE-2 (duplicate `026-` directory in specs/: `026-supervisor-orchestration` + `026-validators-builtins` — orchestration is actually SDD-24, rename via `git mv` during /speckit-implement), F-PRE-3 (SDD-PLAYBOOK SDD-25 row stale — chunk 2 landed scenarios 11a/11b/12 per recent commits but playbook still says "chunk 1 only"), F-PRE-4 (root CONTRIBUTING.md absent — intended action per R-005). **Constitution scope:** **I** (load-bearing FR-014 — whole-tree leak check, SC-005 = 0); **VIII** (load-bearing FR-018 — no AC-protecting test deleted; coverage thresholds preserved; constitutional fenced block + `.github/scripts/coverage-threshold/compute.go` are READ-ONLY); **III/IX/X/XI** indirect (security-critical package removals require R-008 ceremony; renames respect Go idioms; redaction story preserved in README; zero new module deps). **II/IV/V/VI/VII** neutral. **Complexity Tracking:** zero rows — Constitution Check passes clean both before and after Phase 1; the chunk reduces complexity rather than adding it. **Disposition rules (data-model.md Entity 1):** critical=resolved-only and BLOCKS completion; major=resolved or converted-to-issue; minor=any of three (resolved/converted/deferred-to-followup). **Anti-contracts:** NEVER edit `.specify/memory/constitution.md` (out of scope); NEVER edit `.github/scripts/coverage-threshold/compute.go` security-critical-packages list (constitutional FR-016 byte-equality anchor); NEVER tag v0.1.0 (SDD-32's job); NEVER add new behaviour or new public API symbols (FR-016/FR-017); NEVER delete an AC-protecting test (FR-018); NEVER silently drop a doc (FR-019); NEVER convert a critical finding to a GitHub issue (data-model.md disposition gate forbids); NEVER `mv` instead of `git mv` for the specs migration (loses rename detection per R-003). **Files (NEW):** `scripts/check-package-map-vs-code.sh` + `CONTRIBUTING.md` (root) + `specs-archive/<32-subdirs>/` + `specs/033-final-overhaul/findings.jsonl` (populated during /speckit-implement). **Files (EDITED):** `README.md` + `docs/PACKAGE-MAP.md` + `docs/AC-MATRIX.md` + `docs/ARCHITECTURE.md` + `docs/IMPLEMENTATION-PLAN.md` + `docs/SDD-PLAYBOOK.md` + `docs/TESTING-STRATEGY.md` + `internal/supervise/config/example_test.go` + selected `internal/*/*.go` for FR-001/FR-002/FR-003 (volume bounded by audit findings) + `CLAUDE.md` (this marker block). Primary AC: AC-1 (rewritten README is v0.1.0 first impression) + indirectly tightens every AC by ensuring docs match implementation.
<!-- SPECKIT END -->
