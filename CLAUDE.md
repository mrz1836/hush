# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/022-supervise-pidfile-socket/plan.md](specs/022-supervise-pidfile-socket/plan.md) — SDD-22 `internal/supervise` PID file + Unix status socket. **Extends `package supervise`** (alongside SDD-18 `config/`, SDD-19 `state.go`, SDD-20 `child.go`, SDD-21 `refill.go`/`refresh.go`/`grace.go`) with two behaviour files plus build-tagged platform shims: `pidfile.go` (`PidFile` + `AcquirePidFile(path) (*PidFile, error)` + `(*PidFile).Release() error` + sentinels `ErrPidLocked`/`ErrSocketPermsLoose`), `socket.go` (`StatusServer` + locked 3-arg `NewStatusServer(socketPath, store, logger) *StatusServer` + `(*StatusServer).Run(ctx) error` + sentinel `ErrAlreadyRunning` (Clarification 2 / FR-022-14a) + package-private `(*StatusServer).attach(StatusInputs)` mirroring SDD-21's `(*Refiller).attach` precedent + private `statusJSON` DTO with explicit RFC3339 string fields + consumer-defined `StatusInputs` interface), `socket_darwin.go` + `socket_linux.go` (build-tagged test-helper `defaultRuntimeDir()` per OS — production `socket.go` consumes the configured absolute path verbatim, no runtime branching). Two test files: `pidfile_test.go` (8 tests), `socket_test.go` (~14 tests). PidFile: `golang.org/x/sys/unix.Flock(fd, LOCK_EX|LOCK_NB)` on `O_RDWR|O_CREATE|0600`-opened fd; `EWOULDBLOCK` → wrapped `ErrPidLocked` (live owner — FR-022-2); OS-released-on-process-death flock = stale acquired cleanly with **zero retries / zero PID parse / zero `kill(0)` probe** (FR-022-3); PID written via `Truncate(0) + WriteAt` AFTER lock held (FR-022-4); `Release` order = unlock → close → `os.Remove` (best-effort, `IsNotExist` is success — FR-022-5). StatusServer: pre-listen sequence = `ensureParentMode0700(parent)` (creates at `0700` OR refuses with `ErrSocketPermsLoose` on existing laxer mode — FR-022-10 symmetric for both pidfile + socket parents) → `os.Remove(socketPath)` stale-inode cleanup (FR-022-11) → `net.Listen("unix", socketPath)` → `os.Chmod(socketPath, 0o600)` (FR-022-9, never relies on umask); `Run` accept loop runs in caller's goroutine; **3 goroutine types per Run**: (1) accept loop = `Run`'s frame, terminates when `Accept` returns `net.ErrClosed`; (2) watcher = 1 goroutine, owner=`Run`, cancellation=`<-ctx.Done()` OR `<-done`, on wake closes listener + ranges `map[net.Conn]struct{}` under `sync.Mutex` calling `conn.Close()` to force-close in-flight handlers (FR-022-14, Clarification 3, sub-second bound); (3) per-conn handler = 1 goroutine per accepted conn, owner=accept loop, cancellation=watcher's `conn.Close()` propagates as Read/Write error, all joined via `sync.WaitGroup` before `Run` returns (Constitution IX); single-shot `Run` via `s.started bool` under `s.mu` → second call returns `ErrAlreadyRunning` (FR-022-14a); each accepted conn → ONE `Store.Snapshot()` + ONE `inputs` projection per request (FR-022-16). FR-12 ↔ Snapshot field gap (Snapshot has 5 fields: State/ChildPID/LastTransitionAt/Token/Reason; FR-12 needs 10) bridged by `StatusInputs` interface providing 8 missing fields (Name/SessionExpiresAt/RefreshWindowNext/ScopeHealthy/ScopeStale/LastAuthFailure/ChildUptime/DiscordConnected) — wired post-construction via package-private `attach` (Complexity Tracking row 2); pre-attach defaults render shape-conformant document with zero values (`""`, RFC3339 zero-time `"0001-01-01T00:00:00Z"`, `[]string{}`, `null`, `0s`, `false`). `statusJSON` private DTO has **NO `Token` field** — Snapshot.Token bytes never reach the wire (Constitution X / FR-022-13); `child_pid` is `*int` rendering `null` when `Snapshot.ChildPID == 0`; `last_auth_failure` is `*string` rendering `null` when nil; `child_uptime` uses `time.Duration.String()` (e.g., `"8h12m0s"`) matching FR-12 example shape; `scope_healthy`/`scope_stale` initialised to `[]string{}` so JSON never renders `null`. Coverage ≥95% (SC-022-10) verified by `go test -race -cover ./internal/supervise/ -run "PidFile|Socket"`. Lifecycle Scenarios 12 (agent status check) + 14 (duplicate supervisor refused) each have a passing unit test in this chunk (SC-022-1). **Anti-contracts**: NEVER add HTTP (`net/http` not imported — Constitution V); NEVER add TCP (`net.Listen("tcp",...)`, `127.0.0.1:`, `tcp4`/`tcp6` — Constitution V); NEVER add bearer-token / HMAC / signed-cookie auth (Constitution V — FS perms 0600/0700 ARE the auth); NEVER `string(decryptedBytes)` (Constitution X / FR-022-13 — Snapshot.Token never marshalled, marker-byte test `TestSocket_TokenInResponseRedacted` proves no leak); NEVER `init()` or package-level mutable state (Constitution IX); NEVER add new direct `go.mod` dependency (FR-022-19 — `golang.org/x/sys` already in module); NEVER alter any SDD-18 / SDD-19 / SDD-20 / SDD-21 symbol (PACKAGE-MAP locks); NEVER parse PID-file text as control flow (FR-022-3 — flock is sole authority); NEVER silently `chmod` laxer-than-0700 parent dir (FR-022-10 — refuse with `ErrSocketPermsLoose`); NEVER `O_TRUNC` on PID-file open (would corrupt live owner's record on refused acquire — FR-022-2 second clause); NEVER spawn goroutine without owner+ctx+termination+top-frame `recover` (Constitution IX); NEVER let `Run` return before `wg.Wait()` joins every spawned goroutine (SC-022-8); NEVER internally retry `Accept`/`flock`/syscalls — surface wrapped errors verbatim; NEVER bind listener before parent-perms check passes (refuse early); NEVER re-bind on `Run`-already-called instance (`ErrAlreadyRunning`); NEVER include `token` field in JSON DTO; NEVER `omitempty` FR-12 fields (FR-022-12 — every field present, types match exactly); NEVER pass `Snapshot.Token` as a `slog` attribute. Static `TestSocket_NoTCPListenerOrHTTPServer` greps source files for forbidden substrings (`net.listen("tcp"`, `http.server`, `http.listenandserve`, `bearer`, `authorization`) defending against future regressions (SC-022-9). Constitution scope: V (FS perms = auth, no bearer/HTTP/TCP, status visible, loud parent-perms refusal), VIII (TDD-mandatory; ~22 tests authored before implementation; race-clean; ≥95% coverage; status-socket JSON encoding fuzz target #6 owned here, seeded in tasks-phase), IX (sentinel errors `var Err… = errors.New(…)`; error wrap `%w`; no init; no globals; consumer-defined `StatusInputs` interface; goroutine inventory with owner+ctx+termination+recover; pure-Go CGO=0; `golang.org/x/sys` already in module), X (no `string(secret bytes)`; type-driven redaction via SecureBytes.LogValue defense-in-depth; marker-byte capture test asserts no leakage; `slog` records carry mode/identifier only, never connection payload). AC-10 primary (Scenarios 12 + 14).
<!-- SPECKIT END -->
