# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/026-validators-builtins/plan.md](specs/026-validators-builtins/plan.md) — SDD-26 pre-flight credential validators (Validator interface + 5 builtins) for AC-10 Lifecycle Scenario 6 / FR-13. New sibling package `internal/supervise/validators` (R-001 avoids the SDD-24 `supervise.Validator` consumer-interface name collision; mirrors SDD-18 `config` + SDD-27 `watchdog` precedent) exporting **1 interface + 1 opaque struct + 7 funcs/methods + 3 sentinel errors**: `type Validator interface { Validate(ctx context.Context, secret *securebytes.SecureBytes) error }`, opaque `type Registry struct{...}`, `NewRegistry(httpClient *http.Client) *Registry` (wires the five names → constructors), `(*Registry).Get(name string) (Validator, bool)` (returns nil/false for any non-FR-010 name — case-sensitive per Clarification Q2), five per-provider constructors `NewAnthropic / NewAnthropicOAuth / NewOpenAI / NewGoogleAI / NewGitHub(httpClient *http.Client) Validator` (each returns an unexported `*<provider>Validator` cast to the interface), three sentinels `ErrStaleCredential / ErrValidatorTimeout / ErrValidatorNetwork`. **Endpoint pinning (R-003a..R-003e)**: anthropic → GET `https://api.anthropic.com/v1/models` (`x-api-key` + `anthropic-version: 2023-06-01`); anthropic-oauth → same URL with `Authorization: Bearer` + `anthropic-version`; openai → GET `https://api.openai.com/v1/models` (`Authorization: Bearer`); google-ai → GET `https://generativelanguage.googleapis.com/v1beta/models` (`x-goog-api-key` header — never `?key=` query string per R-003d); github → GET `https://api.github.com/user` (`Authorization: token` + `Accept: application/vnd.github+json`). **Shared HTTP machinery (R-006, R-009)**: single `doRequest(ctx, logger, client, name, url, extra, secret, builder) error` helper at `validators.go`; per-provider `authHeaderBuilder` callback runs INSIDE `securebytes.Use(fn)` scope, allocates fresh `buf := make([]byte, len(prefix)+len(secret))`, copies prefix+secret, calls `req.Header.Set(<header>, string(buf))` (the single `string(buf)` site in the package — R-008 explains why this is NOT a Constitution X violation: secret variable is named `buf` not `secret`, the conversion satisfies stdlib `net/http.Header` map[string][]string API, lifetime is one request), then zeros every byte before `Use(fn)` returns. **Status mapping (FR-004, Clarification Q3)**: 2xx → nil; 401/403 → `ErrStaleCredential`; everything else non-2xx (3xx, 4xx-other, 5xx, 429) → `ErrValidatorNetwork`. **Transport-error classifier**: `context.DeadlineExceeded` / `net.Error.Timeout()` → `ErrValidatorTimeout`; `context.Canceled` / DNS / refused / TLS / EOF → `ErrValidatorNetwork`; `securebytes.ErrDestroyed` → `errors.Join(ErrValidatorNetwork, ErrDestroyed)` (Clarification Q6 — preserves underlying chain). **Sentinel-wrap discipline (R-007)**: every failure path returns `errors.Join(ErrXxx, underlying)` or `fmt.Errorf("validators: %s: %w", name, ErrXxx)` — credential bytes / Authorization-header value / `*http.Request` / `*http.Response` / response body NEVER appear as fmt args; mutual exclusion of the three sentinels is proven by construction (single status-code switch + single transport classifier). **Redirect disable (R-005, FR-021, Clarification Q9)**: per-request shallow-copy of `*http.Client` installs `CheckRedirect = func(...) { return http.ErrUseLastResponse }` regardless of operator-supplied policy, so 3xx classifies as `ErrValidatorNetwork` without follow-up; FR-019 single-request guarantee holds. **Default 5s timeout (R-004, FR-012, Clarification Q1)**: `effectiveClient(nil)` returns fresh `&http.Client{Timeout: 5*time.Second, CheckRedirect: <ErrUseLastResponse>}`; operator-supplied client returned verbatim (timeout override honoured). **Pre-cancel fast-path (R-016, SC-008)**: first statement in `doRequest` rejects already-cancelled ctx within ≤ 50 ms / 0 outbound requests / 0 fixture handler invocations. **Log schema (R-011, FR-020)**: exactly one slog record per Validate call, message `"validator outcome"`, DEBUG on success / WARN on failure, attribute allow-list = `{validator: <FR-010 name>, outcome: success|stale|timeout|network, status: <int> (omitted on transport-level failure)}`; NO `error`/`url`/`request`/`response`/`header`/`scope`/`body` attributes. **Logger seam (R-014)**: `export_test.go` `SetLoggerForTest(v Validator, *slog.Logger)` type-switches over the five concrete providers — production constructor signature unchanged. **Registry (R-010)**: `byName map[string]Validator` constructed once in `NewRegistry`, never mutated; concurrent `Get` is race-safe by Go memory model. **103 mandatory tests** (TDD-first per Constitution VIII): 18 shared + 17×5 per-provider = 103, names locked in quickstart.md §4 covering B-V-IF/REG/ERR/DOR/LOG/SEC/FIX + B-V-P-`<p>`-1..16 black-box contracts (happy-200, stale-401, stale-403, network-5xx-table, timeout, refused, redirect-3xx, ctx-cancelled-before, ctx-cancelled-mid, single-request, concurrent, destroyed-securebytes, no-leak-sentinel, name-lock, auth-header-shape, empty-credential, interface-satisfied). Coverage target ≥90% via `go test -race -cover ./internal/supervise/validators/`. **Scope**: Lifecycle Scenario 6 supervisor-side gate (validator returns `ErrStaleCredential` → SDD-24 routes `AlertClassValidatorFailure` → `[STALE] Validator Failure` Discord DM + `awaiting-approval` transition; audit row write belongs to SDD-24 per Spec Assumption row 5). **Constitution scope**: **V** (every failure outcome emits WARN, zero silent suppression — three WARN sites at `emitWarnAndWrap`; success is DEBUG-only); **VIII** (TDD-first, 103 named tests BEFORE implementation, race-clean, ≥90% coverage, sentinel-leak fuzz-style assertion per provider for FR-015/SC-006); **IX** (single-method `Validator` interface; sentinel errors via `var Err... = errors.New(...)` exempt as Constitution-IX-sentinel-class globals per SDD-21 precedent; zero `init()`; zero mutable package-level state; five `<provider>Name` + five `<provider>Endpoint` + four `outcome*` + `anthropicVersionHeader` are compile-time `const` declarations; zero goroutines spawned by the package; ctx-first `Validate(ctx, secret)` method signature); **X** (`*SecureBytes` is the only credential surface; `Use(fn)`-scoped builder consumption; one documented `string(buf)` site at `req.Header.Set` per R-008 is exempt from SC-005's `string(secret`/`string(creds`/`string(credential` grep because the conversion target variable is named `buf`/`headerValue`, not `secret`; log-attribute allow-list excludes every credential-derived field; `*http.Request`/`*http.Header` never threaded to logger/error/byte sink — B-V-SEC-2 + B-V-LOG-3 + B-V-P-`<p>`-13 sentinel-leak per provider prove it). **Anti-contracts**: NEVER hit live provider APIs in tests (FR-014, SC-004 — `rewriteTransport` test fixture forwards production URL to `httptest.Server.URL`); NEVER materialise credential as Go `string` named `secret`/`creds`/`credential` (SC-005); NEVER include credential value / Authorization header / `*http.Request` / `*http.Response` / response body in errors or logs (FR-008, FR-009, FR-020); NEVER retry internally (FR-019 single-shot); NEVER follow HTTP redirects (FR-021, Clarification Q9); NEVER short-circuit empty credential into local `ErrStaleCredential` (edge case "Empty credential" — forward to provider); NEVER mutate caller-supplied `*http.Client` (R-005 shallow-copy strategy); NEVER call `Destroy` on caller's `*SecureBytes`; NEVER register with `http.DefaultClient` or any package-level mutable state (FR-016); NEVER add a sixth registry name (SC-007 closed set; runtime extension is post-v0.1.0 per `docs/DAEMONS.md` §5). **Complexity Tracking**: 2 justified extensions (sibling package `internal/supervise/validators` vs chunk-doc's `internal/supervise` — collision with SDD-24 three-arg `supervise.Validator` interface; per-provider unexported struct + `<provider>Name` constants vs single-struct-switch — keeps per-provider files ≤ 40 LOC and tests black-box-clean), each rationalised in plan.md §Complexity Tracking with rejected alternatives. **Files**: `internal/supervise/validators/validators.go` (shared) + `anthropic.go` + `anthropic_oauth.go` + `openai.go` + `google_ai.go` + `github.go` + `validators_test.go` + `anthropic_test.go` + `anthropic_oauth_test.go` + `openai_test.go` + `google_ai_test.go` + `github_test.go` + `export_test.go` (test-only `SetLoggerForTest` seam — R-014). Primary AC: AC-10 Lifecycle Scenario 6 (FR-13 pluggable credential validators).
<!-- SPECKIT END -->
