# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/031-release-gates/plan.md](specs/031-release-gates/plan.md) — SDD-31 release gates (CI / repo-level) for AC-9. Three NEW workflow files plus one Go tool: `.github/workflows/ci.yml` (per-PR matrix macos-arm64 + linux-amd64 on Go 1.26.1 — eleven gates: format-check FR-004, lint FR-005, test-race-with-cover FR-006, pre-commit FR-007, govulncheck FR-008, gitleaks FR-009, fuzz-smoke FR-010 30 s × 6, codecov-upload FR-011, coverage-threshold FR-012-016, no-vendor FR-017, no-CGO FR-018/019) + `.github/workflows/fuzz-cron.yml` (nightly `0 7 * * *` UTC linux-only matrix-by-target 5 min/target configurable via `workflow_dispatch` input, crashing inputs preserved as 30-day artefacts per FR-022) + `.github/workflows/release.yml` (tag → goreleaser + cosign keyless via OIDC `id-token: write`, FR-023-027) + `.github/scripts/coverage-threshold/{main.go,compute.go,compute_test.go}` (stdlib-only Go tool reading cover.out + threshold check + FR-016 byte-equality self-test against `.specify/memory/constitution.md` fenced block). **Locked HOW (chunk-doc SDD-31 + plan research.md):** (R-001) **Canonical six fuzz targets** supersede the chunk-doc's stale `FuzzServerTOML` listing per spec FR-010 supersession clause: `FuzzVaultDecode` (`internal/vault`) + `FuzzJWTValidate` (`internal/token`) + `FuzzECIESDecrypt` (`internal/transport/ecies`) + `FuzzVerifyRequest` (`internal/transport/sign`) + `FuzzSuperviseTOML` (`internal/supervise/config`) + `FuzzStatusJSON_Encode` (`internal/supervise`) — all six exist in the codebase; same list lives in ci.yml fuzz-smoke step and fuzz-cron.yml matrix (FR-029 lockstep). (R-002) **Constitution PATCH 1.1.0 → 1.1.1**: append fenced block `<!-- security-critical-packages: BEGIN/END -->` to Principle VIII listing seven packages (`internal/keys`, `internal/vault`, `internal/vault/securebytes`, `internal/token`, `internal/transport/sign`, `internal/transport/ecies`, `internal/audit`); coverage-threshold tool's `securityCriticalPackages` constant must byte-equal that block, enforced by `TestSecurityCriticalListMatchesConstitution` (FR-016). (R-003) **fuzz-cron is linux-amd64 only** — OS doesn't change fuzz coverage, doubling onto macOS wastes minute budget; per-PR smoke already covers both OSes. (R-004) **cosign keyless via GoReleaser `signs:` block** with `artifacts: checksum` (sign manifest only — not every binary); release.yml grants `id-token: write` so OIDC subject binds to release-tag ref (FR-025 lock). (R-005) **No `.golangci.json` edit** — existing linter set already covers Principle IX (`gochecknoglobals`, `gochecknoinits`, `containedctx`, `contextcheck`, `noctx`, `errcheck`, `errorlint`, `err113`, `errname`, `gosec`). (R-006) **`.github/scripts/coverage-threshold/`** location is spec-mandated; CI uses explicit-path invocation (`go run ./.github/scripts/coverage-threshold ...`, `go test ./.github/scripts/coverage-threshold/...`) because `./...` skips dot-prefixed dirs. Three-file split: `main.go` (≤40 lines flag+exit), `compute.go` (≤120 lines pure-fn parser + checker, stdlib-only), `compute_test.go` (table-driven). Exit codes: 0 pass / 1 threshold breach / 2 malformed cover.out (FR-015) / 3 constitution-list divergence (FR-016). (R-007) **Legacy `fortress-*.yml` workflows stay** — spec assumption ("any pre-existing workflow that duplicates a gate is out of this feature's scope to remove, but MUST NOT relax the constitutional bar"); new three workflows are the AC-9 owner. **`.goreleaser.yml` edits:** promote `env: [CGO_ENABLED=0]` to top-level (currently builds[0]-scoped) + add `linux` to `builds[0].goos` (currently `[darwin]` only — FR-024 adds linux-amd64 + linux-arm64) + new `signs:` block (cosign keyless, manifest-only). **`.govulncheck-allow.yml`** new file at repo root, schema `{version: 1, vulns: [{id, justification, expires}]}` — FR-008 single source of truth (PR descriptions non-authoritative). **Required GitHub branch-protection check names:** `ci / gates (macos-arm64)` + `ci / gates (linux-amd64)` + `ci / coverage-threshold` + `ci / coverage-upload`. **Per-PR job graph:** `gates` (matrix × 2 OS pairs, fail-fast false, runs all 11 gates) → `coverage-threshold` (linux-only, downloads cover.out artefact, self-tests + threshold check) → `coverage-upload` (linux-only, codecov-action@v4 with `fail_ci_if_error: true`). **Per-PR pipeline budget:** ≤ 12 min cold (~6 min cache-warm); 30 s × 6 fuzz = 180 s; format+lint+test:race dominate. **Nightly cron budget:** matrix-by-target (6 parallel) × 300 s ≈ 10 min wall-clock vs 30 min sequential. **Constitution scope:** **VIII** (every FR-004…FR-016 gate wired as required check; nothing `continue-on-error`; coverage threshold 90 %/100 % enforced; six-fuzz-target list canonical); **IX** (coverage-threshold tool: package `main`, no `init()`, no globals, `%w` error wrap, sentinel errors `ErrMalformedCoverOut`/`ErrCoverageBelowThreshold`/`ErrConstitutionMismatch`, stdlib-only imports, table-driven tests); **X** (gitleaks gate inverts the redaction concern — asserts repo carries no plaintext secret); **XI** (zero new Go direct dependencies — govulncheck/gitleaks/cosign/GoReleaser-Action are CI runners not Go modules; CGO=0 enforced via job-level `env: CGO_ENABLED: "0"` + no-cgo grep gate; `/vendor` forbidden by no-vendor gate). **Anti-contracts:** NEVER skip a fuzz target to fit a time budget (FR-021 — move to cron, never drop); NEVER disable race detector (FR-006 chunk-doc anti-contract); NEVER let CGO_ENABLED be unset or non-zero (FR-019 belt-and-braces: job-env + .goreleaser.yml top-level env + no-cgo grep); NEVER add a `/vendor` directory (FR-017); NEVER mark a gate `continue-on-error: true` (FR-003/028); NEVER use PR description for vulnerability waivers (FR-008 — `.govulncheck-allow.yml` is single source of truth); NEVER sign per-binary (FR-025 — manifest-only); NEVER add an eighth security-critical package without updating BOTH the constitution fenced block AND `securityCriticalPackages` constant in same PR (FR-016 byte-equality enforces). **Complexity Tracking:** zero rows — Constitution Check passes clean both before and after Phase 1. **Files:** `.github/workflows/ci.yml` + `.github/workflows/fuzz-cron.yml` + `.github/workflows/release.yml` + `.github/scripts/coverage-threshold/main.go` + `.github/scripts/coverage-threshold/compute.go` + `.github/scripts/coverage-threshold/compute_test.go` + `.govulncheck-allow.yml` (NEW); `.goreleaser.yml` + `.specify/memory/constitution.md` + `docs/AC-MATRIX.md` + `docs/PACKAGE-MAP.md` (EDITED). Primary AC: AC-9 (test coverage + fuzz release gate — workflow files cited in AC-MATRIX row).
<!-- SPECKIT END -->
