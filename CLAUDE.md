# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/029-deploy-artifacts/plan.md](specs/029-deploy-artifacts/plan.md) — SDD-29 operator-facing deploy artefacts (launchd plist + systemd unit + idempotent install.sh + generic supervise-launch.sh.template) for AC-1 + AC-6 + AC-10. Four committed files under `deploy/` plus Go integration tests under `tests/deploy/` gated by `//go:build integration`. **Locked HOW (chunk-doc SDD-29 lines 157–185 + plan research.md):** (1) `deploy/hush.plist` — Label `com.hush.server` (product identifier, not operator-specific — R-004), `ProgramArguments=["/usr/local/bin/hush","serve","--config","/usr/local/etc/hush/config.toml"]`, `UserName=_hush`, `RunAtLoad=true`, `KeepAlive=true`, `StandardOut/ErrPath=/usr/local/var/log/hush.{out,err}.log`; (2) `deploy/hush.service` — `[Unit] Description=hush — Discord-gated secrets broker / After=network-online.target / Wants=network-online.target`, `[Service] Type=simple / User=@HUSH_USER@ / ExecStart=/usr/local/bin/hush serve --config /etc/hush/config.toml / Restart=on-failure / RestartSec=5s / NoNewPrivileges=true / ProtectSystem=strict / ProtectHome=true / PrivateTmp=true`, `[Install] WantedBy=multi-user.target` (install.sh sed-substitutes `@HUSH_USER@` at copy time — R-011); (3) `deploy/install.sh` — bash 3.2+ compatible (`#!/usr/bin/env bash` + `set -euo pipefail`), reads 5 env vars `PREFIX` (default `/usr/local`), `HUSH_USER` (default `_hush` macOS / `hush` Linux — R-003), `HUSH_STATE_DIR` (default `/usr/local/var/hush` macOS / `/var/lib/hush` Linux per spec Clarification A2), `HUSH_INSTALL_ROOT` (test-only staging prefix per R-002 — operators leave unset), `HUSH_SOURCE_BIN` (default `./hush`); 7-step flow per data-model.md §3 (1. create `${HUSH_USER}` via `dscl`/`useradd --system` idempotently; 2. `install -d -m 0700 -o ${HUSH_USER} ${STATE_DIR}` per FR-002a + Constitution X; 3. macOS-only `tmutil addexclusion ${STATE_DIR}` per FR-002 + Constitution XI non-negotiable, hard-fails exit-code-4 if `tmutil` missing per R-006; 4. `install -m 0755 ${HUSH_SOURCE_BIN} ${BIN_PATH}`; 5. `install -m 0644` service file to `/Library/LaunchDaemons/hush.plist` (macOS) or `/etc/systemd/system/hush.service` (Linux) with `@HUSH_USER@` substitution on Linux + optional `<string>_hush</string>` substitution on macOS if `HUSH_USER` overridden; 6. Linux-only `systemctl daemon-reload`; 7. print byte-identical-across-reruns banner with `security add-generic-password -a "${HUSH_USER}" -s "hush-vault-passphrase" -T "${RESOLVED_BIN_FOR_ACL}" -U -w "<YOUR-PASSPHRASE>"` invocation per FR-003+SC-004 — install.sh creates ZERO Keychain entries / reads ZERO passphrases per FR-003 absolute lock; 5 exit codes `0=success/idempotent-noop / 1=generic / 2=bad-input / 3=insufficient-privilege / 4=missing-external-tool`); (4) `deploy/supervise-launch.sh.template` — 3 substitution placeholders `<NAME>` (logical daemon name, informational comment-only), `<KEYCHAIN_ITEM>` (Keychain item name, informational comment-only), `<CONFIG_PATH>` (absolute supervisor TOML path, appears in exec line); pre-flight self-grep guard that exits 78 (`EX_CONFIG`) if any placeholder unsubstituted per spec edge-case + R-010 (guard line uses string concatenation `'<''NAME>'` so it doesn't match itself); single-line core `exec /usr/local/bin/hush supervise --config <CONFIG_PATH>`; `hush request --exec` appears EXACTLY ONCE inside explicit DO-NOT warning comment (FR-019 — grep filters `^[[:space:]]*[^#]` so commented occurrence is excluded). **Test harness (R-001):** `tests/deploy/install_test.go` + `tests/deploy/smoke_test.go` both `//go:build integration`; 5 install tests `TestDeploy_InstallIdempotent` (FR-001/002/025 — runs install.sh twice in `t.TempDir`, captures stdout, snapshots tree+modes+content-hashes, asserts `bytes.Equal(stdout1,stdout2)` + `snap1==snap2` + exactly one `tmutil addexclusion` line in stub log + stdout1 contains `-T "${RESOLVED_BIN_FOR_ACL}"` exactly once + no `-T "*"` and no `-A` anti-contract) + `TestDeploy_InstallRefusesUnsupportedOS` (FR-005, exit 2 via `HUSH_FORCE_OS=plan9` test escape hatch) + `TestDeploy_InstallRefusesMissingBinary` (FR-007, exit 2) + `TestDeploy_InstallRefusesMissingTmutil` (FR-002 hard-fail, exit 4 with PATH stripped of tmutil) + `TestDeploy_InstallBannerByteIdentical` (focused FR-001 sub-contract); 5 smoke tests `TestDeploy_PlistParsesAsXML` (FR-012 — `encoding/xml` decode + UserName!=root + ProgramArguments[0]==/usr/local/bin/hush) + `TestDeploy_ServiceParsesAsINI` (FR-016 — sections `[Unit]/[Service]/[Install]` + `User=@HUSH_USER@` + `ExecStart=/usr/local/bin/hush…`) + `TestDeploy_LauncherTemplateExecsSupervise` (FR-018/019/026 — `bash -n` parse + contains `hush supervise` + zero non-comment matches for `hush request --exec` + all 3 placeholders present) + `TestDeploy_NoOperatorSpecificNames` (FR-009/013/017/022 + SC-007 — denylist grep `openclaw|hermes|mrz|100\.90\.|tag:trusted` across all 4 files, zero matches) + `TestDeploy_AllShellFilesParse` (FR-024/SC-008 — `bash -n` on every `.sh`+`.template`); fixtures `tests/deploy/testdata/tmutil_stub.sh` (records argv to `${TMPDIR}/tmutil.log`, prepended to PATH for macOS-flavour run) + `tests/deploy/testdata/fake-hush` (zero-byte exec used as `HUSH_SOURCE_BIN`). Runner: `magex test:race -tags=integration -run TestDeploy_ ./tests/deploy/...` (per chunk-doc Prompt 5 step 4). **Mode/ownership matrix (R-012):** binary `0755 root:wheel/root` + plist `0644 root:wheel` + unit `0644 root:root` + state-dir `0700 ${HUSH_USER}:(system)` + future-vault-files `0600 ${HUSH_USER}:(system)`. **Constitution scope:** **I** (zero operator-specific names — `com.hush.server` is product identifier; `<NAME>/<KEYCHAIN_ITEM>/<CONFIG_PATH>` are placeholders; `HUSH_INSTALL_ROOT` is operator-facing but encodes nothing operator-specific so no violation); **IV** (launcher template execs `hush supervise` exclusively; `hush request --exec` is comment-only and DO-NOT-warned — would re-prompt every restart and defeat supervisor TTL); **V** (5 documented exit codes; missing-binary/unsupported-OS/missing-tmutil/unsubstituted-placeholder all fail loudly with stderr `install.sh: <stage>: <reason>` format); **IX** (Go integration tests stdlib-only, no `init()`, no globals, `t.TempDir()` per test, table-friendly — bash files out of scope since principle reads "every line of Go in this repo"); **X** (install.sh handles ZERO secret material — FR-003 absolute lock asserted by stdout `-T` grep; state-dir mode `0700`; banner is byte-identical across reruns); **XI** (zero new Go direct dependencies — `encoding/xml` covers plist parse on every platform; `tmutil addexclusion` unconditional on macOS, missing-tmutil → exit-4-not-skip; state-dir non-root). **Anti-contracts:** NEVER let install.sh create/read Keychain entries (FR-003 — banner-only `security add-generic-password` invocation); NEVER hard-code operator-specific names in any committed file (FR-009/013/017/022 — denylist grep); NEVER use `hush request --exec` in launcher template active lines (FR-019 — non-comment grep); NEVER silently skip `tmutil addexclusion` on macOS (Constitution XI non-negotiable — hard-fail if tmutil missing); NEVER run as root (FR-006/010/014 — non-root `${HUSH_USER}` mandatory); NEVER back up `${HUSH_STATE_DIR}` (Constitution XI ephemeral vault); NEVER introduce a new Go dependency (Constitution XI — stdlib only); NEVER use bash 4-only constructs in install.sh or template (R-009 — bash 3.2+ macOS system-bash floor). **Complexity Tracking:** zero rows — Constitution Check passes clean. Two clarification-grade design choices (`HUSH_INSTALL_ROOT` test-only staging knob per R-002 + launcher pre-flight grep guard per R-010 / spec edge-case) are not chunk-doc-API extensions and not Constitution violations; documented inline in plan §Complexity Tracking with rationale. **Files:** `deploy/hush.plist` + `deploy/hush.service` + `deploy/install.sh` + `deploy/supervise-launch.sh.template` + `tests/deploy/install_test.go` + `tests/deploy/smoke_test.go` + `tests/deploy/testdata/tmutil_stub.sh` + `tests/deploy/testdata/fake-hush`. Primary AC: AC-1 (CLI surface — `hush serve` referenced by plist/unit) + AC-6 (Keychain ACL — banner's `-T <binary-path>` discipline) + AC-10 (daemon lifecycle — launcher template execs `hush supervise`, defeats interactive-re-prompt failure mode).
<!-- SPECKIT END -->
