# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/025-lifecycle-harness/plan.md](specs/025-lifecycle-harness/plan.md) — SDD-25 lifecycle integration harness + 15 named scenarios; explicit AC-10 owner (also lifts AC-9). **Pure test deliverable** built ON TOP of every locked `internal/*` package (SDD-01..SDD-23); no production code is altered. New top-level `tests/integration/` tree, all files build-tagged `//go:build integration`. Six harness files under `tests/integration/harness/` (`vault.go`, `server.go`, `discord.go`, `supervisor.go`, `child.go`, `log_capture.go`) plus two test files (`tests/integration/lifecycle_test.go` — suite-wide TestMain + 15 stubs; `tests/integration/scenarios_test.go` — 15 scenario bodies named `Test_Scenario_NN_<slug>` per the locked table). Each scenario asserts FOUR contracts in order: (A) final state matches docs/LIFECYCLE-SCENARIOS.md (compound 4-tuple for Scenario 1 per Clarification 4); (B) audit subsequence + chain continuity (subsequence per Clarification 1 — intervening events tolerated; hash-chain continuity via `internal/audit.Verify`); (C) status-socket JSON shape matches SDD-22 locked `statusJSON` DTO verbatim with field-value projection per scenario (supervisor scenarios only — Scenario 1 + Scenario 10/Interactive subtest skip); (D) `AssertSentinelAbsent` across SIX byte streams (operational slog, audit JSONL, raw status-socket bytes, Discord-stub alert payloads, child stdout/stderr, error-message strings). External boundaries mocked: Discord via `testutil.DiscordStub` adapted to `server.Approver` via the existing `stubAsApprover` pattern from `internal/server/claim_handler_integration_test.go`; validator upstream HTTP (Anthropic / Anthropic-OAuth / OpenAI / GitHub / Google AI) via per-validator `httptest.Server` + `http.RoundTripper` hostname-rewrite; NTP via `server.Deps.ClockSyncProbe` seam; wall clock via injected `func() time.Time` PLUS a controllable `supervise.Clock` implementation. **Refresher clock seam strategy**: harness does NOT call `Refresher.Run()` — instead invokes the `refill` callback directly at scripted instants (Refresher's tick loop is covered by its own unit tests via package-private `setClockForTest`; adding a public `WithClock` option would break the SDD-21 "3 ctors + 4 methods + 2 sentinels + Evict" lock). **Child process strategy**: `os.Executable()` re-invocation in `--integration-child-mode` (matches `internal/supervise/child_test.go` precedent; NO new `cmd/integration-child` binary). **Goroutine-leak detection**: `runtime.NumGoroutine` pre/post snapshot + bounded `runtime.Gosched()` poll (max 100 iterations) — NO `time.Sleep` to wait for documented transitions per FR-025-16. **Audit subsequence algorithm**: classic O(n+m) two-pointer subsequence check + `audit.Verify` for hash-chain continuity. **Validator-upstream HTTP mocks**: one `httptest.Server` per validator, `http.RoundTripper` rewrites real hostname → loopback, programmable handlers per scenario. **Status-socket client**: raw Unix-domain dial + verb-dispatch (`status` default verb returns FR-12 doc; `refresh\n` verb returns `{ok,error}` ack). Scenario 9 ships as TWO top-level functions `Test_Scenario_09_OvernightExpiry_Strict` + `Test_Scenario_09_OvernightExpiry_Grace` (counts as ONE row in AC-10's 15-scenario contract per FR-025-5). Scenario 10 ships as ONE function with TWO subtests `Interactive` + `Supervisor` per FR-025-5a. Total `Test_Scenario_*` top-level symbols: 17 (15 scenarios + 1 extra for Scenario 9 grace variant + 1 extra for... wait no, Scenario 14 is one function with one subtest each so it's actually 16 — see spec.md table for the exact list). **Anti-contracts**: NEVER `t.Skip` to soften a missing audit event (FR-025-7 + edge-case row 1; audit log is normative contract); NEVER `time.Sleep` to drive a documented transition (FR-025-16; use `runtime.Gosched()` bounded poll or `Clock().Advance()`); NEVER reach a non-loopback host outside registered httptest endpoints (FR-025-13; `http.Client.Transport` is a `RoundTripper` returning error on disallowed hosts); NEVER touch `~/.hush/` or any path outside `t.TempDir()` (FR-025-22); NEVER `t.Parallel()` at scenario top (FR-025-21; internal `t.Parallel` allowed only on disjoint state); NEVER add a new exported symbol to any `internal/*` package (SDD-01..SDD-23 PACKAGE-MAP locks); NEVER add `setClockForTest` or any package-private seam access from outside the same Go package (the harness consumes the public surface only); NEVER call `Refresher.Run()` and then expect deterministic tick timing (drive `refill` callback directly); NEVER add a 7th harness file (chunk contract enumerates exactly 6); NEVER add a `//go:build integration` file inside any `internal/*` directory (the integration suite lives under `tests/integration/` only); NEVER stub a production package (validators, watchdog, alerts) — when a provider chunk is `pending`, the scenario fails until that chunk ships, per spec FR-025-3 + Assumptions; NEVER `init()` or new package-level mutable `var` in the harness (Constitution IX); NEVER spawn a goroutine in the harness without owner+ctx+termination+top-frame `recover()` (Constitution IX); NEVER unmarshal-then-re-marshal status-socket bytes before sentinel-absence check (FR-025-26 covers *captured* bytes; re-marshalling could hide a leak); NEVER strict-equal recorded == documented audit events (Clarification 1 requires subsequence); NEVER add new `go.mod` direct dependency (Constitution XI — stdlib + `stretchr/testify` already in module suffice); NEVER make Scenario 1's "final state" assertion a single value (FR-025-6 + Clarification 4 require compound 4-tuple: health flags + child exit code + token-store state + DM count == 1). Constitution scope: **VIII** (TDD-mandatory; suite IS the AC-10 contract; every scenario has 4 mandatory assertions; race detector enabled; AC-9 + AC-10 evidence row updated at implement-phase completion), **IX** (no `init`; no globals; every goroutine owner+ctx+termination+top-frame `recover`; goroutine-leak detector at scenario teardown via `runtime.NumGoroutine` snapshot), **X** (`AssertSentinelAbsent` covers 6 captured byte streams per scenario; every secret constructed via `testutil.SentinelSecret(N)`; Discord-stub alert payloads recorded and asserted for scope-name-only — never the secret value, per FR-025-27). Primary AC: AC-10 ALL 15 scenarios green + AC-9 test-infra completeness; coverage target 15/15 scenarios green, suite < 120s, 5 consecutive runs flake-free, zero race-detector findings.
<!-- SPECKIT END -->
