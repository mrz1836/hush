# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/026-supervisor-orchestration/plan.md](specs/026-supervisor-orchestration/plan.md) — SDD-24 supervisor orchestration glue (ACTIVATED by SDD-25 gap). Production orchestrator that composes locked SDD-19..22 primitives (`Store`, `Child`, `Refiller`, `Refresher`, `Grace`, `StatusServer`, `PidFile`) into the documented daemon lifecycle: pidfile → StatusServer + Refresher goroutines → boot-retry against Tailscale + vault `/hz` → signed `/claim` (SDD-08 canonical-sign) → JWT into Store → `Refiller.Refill` → injected validators → child env build → `NewChild.Start` → `Child.Wait` loop with `0` / non-zero-non-`78` / `78` dispatch → Refresher window-tick swap → SIGTERM clean shutdown. **Scope decision: Option C (deviation from chunk-doc Option B)** — orchestrator lives inside `package supervise` itself at `internal/supervise/lifecycle.go` + 4 siblings (`lifecycle_interfaces.go`, `lifecycle_boot.go`, `lifecycle_child.go`, `lifecycle_refresh.go`, `lifecycle_audit.go`), NOT in a sub-package. Reason: chunk-doc Option B literal (sub-package `internal/supervise/lifecycle/`) cannot reach the package-private `(*Refiller).attach` (SDD-21) or `(*Store).setTokenForTest` (SDD-19) — the documented post-construction wiring seams. Option B + exported `Attach`/`SetToken` would mutate the SDD-21 PACKAGE-MAP lock string ("three ctors + four methods + two sentinels + Evict" verbatim) AND the SDD-19 lock. Option C keeps every SDD-19..22 PACKAGE-MAP lock intact and adds ONLY new SDD-24-owned exported symbols (`Lifecycle`, `Deps`, `NewLifecycle`, `Run`, `Validator`, `Alerts`, `AlertClass`, `AlertPayload`, `Watchdog` + sentinels). Cli shim at `internal/cli/supervise.go` shrinks from ~335 LOC to ~80 LOC — Constitution VII outcome identical to chunk Option B. **Goroutine inventory (5 total, all owner+ctx+termination+recover per Constitution IX)**: 3 orchestrator-owned (`childWaitLoop` invokes `Child.Wait` and sends ExitCode; `claimRefreshLoop` consumes refresh-tick chan and submits fresh /claim; `mainLoop` dispatches childExit/refreshResult/refreshVerb/ctx.Done) + 2 carry-over (`StatusServer.Run`, `Refresher.Run`); all join via `Lifecycle.wg` before Run returns. **Boot retry**: exp backoff 500ms initial × 2.0 multiplier with ±20% jitter, cap 30s/attempt, per-attempt HTTP probe ≤ 2s (A-026-2), total budget `cfg.BootRetryTimeout` (default 10m); Tailscale via injected `Deps.TailscaleProbe` (default backed by SDD-10 InterfaceLister); vault via `GET <server>/hz` 2s timeout; exhaustion → emit `AlertClassBootTimeout`, append `ActionSupervisorBootTimeout`, return wrapped `ErrBootTimeout`. **/claim submission**: caller-side signing via `sign.CanonicalJSON` + `sign.Sign` (SDD-08); JWT stored via package-private `Store.setToken` (renamed from `setTokenForTest`); 503 body parsed for `error == "discord_unavailable"` (FR-026 Clarification 5) → `AlertClassDiscordUnavailableOnClaim` + retry inside boot budget; other 5xx/network → generic boot retry; 401/4xx-non-401 terminal. **AlertClass enum LOCKED at exactly 10 values** per spec FR-026-016: `ValidatorFailure`, `Exit78`, `VaultRejectedJWT`, `RefillFailed`, `DiscordUnavailableOnClaim`, `RefreshDenied`, `RefreshTimeout`, `GraceEntered`, `LogPatternMatch`, `BootTimeout`. SDD-28 supplies rendering on top — MUST NOT extend enum. `AlertPayload` carries 3 string fields (Scope, ErrorClass, Reason) — structurally cannot hold secret bytes. **AlertClass ↔ emission-site mapping** (FR-026-013): orchestrator emits 9 of 10 classes directly; `LogPatternMatch` is emitted by SDD-27's Watchdog impl via the same `Deps.Alerts` handle. **Validator interface** at consumer (Constitution IX single-method): `Validate(ctx, scope, *SecureBytes) error`; nil → no-op default; `Deps.Validators map[string]Validator` keyed by scope; SDD-26 supplies 5 builtins. **Watchdog interface** at consumer: `OnStderrLine(ctx, line []byte)`; wired via `lineSplittingWriter` (tee + line-buffered adapter) passed as `ChildConfig.Stderr` — SDD-20's single drain still owns the read side (FR-026-030); 64 KiB per-line cap matches SDD-20 ring. Alert-only — MUST NOT influence state machine (Constitution V). **Audit reconciliation (FR-026-014)**: append 12 new constants to `internal/audit/chain.go` (file is append-only per its line-33 header): `ActionSupervisorSessionClaimed`, `…SessionRefreshed`, `…SilentRefill`, `…ChildCleanExit`, `…ChildExitCrash`, `…ChildExit78`, `…AwaitingApproval`, `…StaleAlert`, `…GraceEntered`, `…GraceExited`, `…BootTimeout`, `ActionClientRefreshInvoked`. Reused: `ActionSecretRetrieved`, `ActionDiscordDisconnected`, `ActionDiscordReconnected` (all emitted by other code paths, not the orchestrator itself). **SPEC.md §FR-14 amendment (Plan ADR-1)** — implement-phase adds the two missing names (`supervisor_child_exit_crash`, `supervisor_boot_timeout`) so §FR-14 ↔ chain.go ↔ orchestrator emissions agree 1:1 (SC-026-008). **Status-socket refresh-verb behaviour per state** (Clarification 4): boot-retry/fetching/stopped → reject with `{"ok":false,"error":"<state>"}\n`; awaiting-approval → drive full refill+validate+restart; running/grace-restart → coalesce with in-flight refill via existing `refreshCoalescer` (FR-023-22a not regressed). **Shutdown**: 10s SIGTERM grace before SIGKILL escalation, 15s hard ceiling on Run-exit budget (A-026-3 ≤ SDD-22 socket-shutdown ceiling). Sequence: ctx cancel → `Child.Forward(SIGTERM)` → 10s wait → `Child.Forward(SIGKILL)` → 5s wait → `wg.Wait()` → cli shim's `defer pidfile.Release()`. **Secret-bytes audit** (Constitution X): exactly one NEW `string(*SecureBytes)` site introduced by SDD-24 — child env build at fork boundary (`Grace.Get(scope).Use(func(b){ env = append(env, scope+"="+string(b)) })`); env slice zeroed after `Child.Start` returns; mirrors existing SDD-20 `ChildConfig.Env []string` contract. The JWT bearer-header site inside `Snapshot.Token.Use` is the existing Constitution-X-permitted site. **Test contract**: 19 unit + 3 integration cases pinned in plan §11 — one per exit-code branch, one per boot-retry branch, `TestLifecycle_NoBusinessLogicLeakage` (grepper for state literals, raw 78, runtime.GOOS), `TestLifecycle_NoSentinelLeakage` (5-stream sweep), `TestSpecFR14AuditSync` (SC-026-008 docs↔code grep). Coverage gate ≥85% line on lifecycle*.go. **Anti-contracts**: NEVER mutate any SDD-19..22 PACKAGE-MAP lock (no new exported methods on existing types — Option C avoids by living in same package); NEVER pre-define validator/alert/watchdog implementations (SDD-26/27/28 own those); NEVER add new `go.mod` direct dependency (FR-026-027); NEVER `string(*SecureBytes)` outside Refiller JWT bearer-header AND child-env build sites (FR-026-028); NEVER spawn a goroutine without owner+ctx+termination+top-frame recover (FR-026-029); NEVER add second drain on Child.Stdout/Stderr (FR-026-030); NEVER `init()` or new package-level mutable vars beyond `var Err… = errors.New(…)` (FR-026-031); NEVER add `case StateRunning`, raw `78` literal, `runtime.GOOS`, or state-string literal in orchestrator file (FR-026-023; state-table reasoning owned by SDD-19, exit-78 by SDD-20); NEVER `t.Skip` to soften missing audit events; NEVER `time.Sleep` to drive documented transitions (use `runtime.Gosched()` bounded poll or `Clock()` injection); NEVER extend `AlertClass` enum without spec amendment (10 values LOCKED). Constitution scope: **IV** (silent refill within session TTL; refresh swap leaves child PID unchanged; grace cap honoured), **V** (every documented alert site fires; 3 distinct `[STALE] …` paths; status socket reflects every transition), **VII** (cli shim ≤ 80 LOC; orchestrator in `internal/supervise/`; no platform branching in orchestrator file), **VIII** (TDD-mandatory; ≥85% line coverage on lifecycle*.go; race detector; per-branch tests; SC-026-008 docs-sync test), **IX** (5 goroutines all owner+ctx+termination+recover; no `init()`; no globals; consumer-defined interfaces; context first param), **X** (no `string(secretBytes)` outside 2 documented sites; AlertPayload structurally cannot carry secret; sentinel-leak test on 5 streams). Primary AC: AC-10 precondition unblocked — 14 of 15 SDD-25 scenarios reach final state once this chunk lands; SDD-25 pause banner removed at SDD-24 implement-phase completion. **Complexity Tracking carries 2 deviations**: (1) Option C scope decision (sub-package literal infeasible); (2) one new `string(*SecureBytes)` site at OS-execve fork boundary (unavoidable per Go stdlib `[]string` Env contract).
<!-- SPECKIT END -->
