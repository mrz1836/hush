# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/021-supervise-refill-refresh/plan.md](specs/021-supervise-refill-refresh/plan.md) — SDD-21 `internal/supervise` refill + refresh + grace cache. **Extends `package supervise`** (alongside SDD-18 `config/` sub-package, SDD-19 `state.go`, SDD-20 `child.go`) with three behaviour files: `refill.go` (`Refiller` + `NewRefiller(client, store, logger)` + `Refill(ctx, scopes)` + sentinel errors `ErrJTIUnknown`/`ErrBootTimeout`), `refresh.go` (`Refresher` + `NewRefresher(window, ttl, refill, logger)` + `Run(ctx)` + window+T-30 tick scheduler), `grace.go` (`Grace` + `NewGrace(window, enabled)` + `Get`/`Set`/`Evict` — locked API extended by **one** method `Evict` per Clarification 5 / FR-021-16). Three test files: `refill_test.go` (7 tests), `refresh_test.go` (9 tests), `grace_test.go` (10 tests), plus `helpers_test.go` (`fakeClock`, `roundTripFunc`). Refill: GET `<server>/s/<name>` per scope with `Authorization: Bearer <jwt>` from `store.Snapshot().Token.Use(func(b []byte){...})`; HTTP 401 + body `{"error":"unknown_jti"}` → wrapped `ErrJTIUnknown` (FR-021-3); other failures → wrapped underlying error (FR-021-4); `committed bool` rollback pattern destroys all decrypted `*SecureBytes` on ANY error (FR-021-5, R-007); 64 KiB body cap; ciphertext zeroed post-decrypt; **never internally retries** (boot retry caller-managed at SDD-23, R-010); ECIES private key + Grace handle + server URL wired post-construction via package-private `(*Refiller).attach` to honour locked 3-arg constructor (Complexity Tracking row 2). Refresher: parses canonical "HH:MM-HH:MM" eagerly into 4 ints; tick loop over `time.Timer`; in-memory `lastFiredDay` (calendar-date) + per-session `t30Fired bool` flags (FR-021-10, never persisted, lost on process restart per Clarification 1); fires once on init if inside window AND `lastFiredDay != today`; wall-clock predicate + monotonic timer split (FR-021-11, R-003); **non-nil `refill` callback error counted as issued** — log WARN, advance `lastFiredDay`, never retry within window (FR-021-11a, Clarification 4); single-shot `Run` via `sync.Once`; ctx-cancellation is the sole exit (FR-021-9, RF-7); zero sub-goroutines — `refill` callback runs inline (R-014). Grace: `map[string]graceEntry` under `sync.RWMutex`; effective TTL `min(window, 4h)` applied at `NewGrace` (FR-021-12, GR-1); `enabled=false` OR `window<=0` → silent no-op `Set` (caller retains ownership, R-009 / FR-021-14); `Set`-overwrite destroys prior entry (FR-021-13); **lazy-evict on `Get`** under write-lock with re-check after upgrade (R-008 final, replaces SDD-21's `TestGrace_SweeperDestroysExpired` with `TestGrace_LazyEvictsOnGetAfterTTL` — same FR-021-13 destruction semantics, no goroutine); `Evict` destroys + deletes; absent-name Evict is silent no-op (Clarification 5); zero Grace-owned goroutines (Constitution IX); no logger field (locked 2-arg constructor). **Single goroutine type owned by chunk**: Refresher tick loop, owner = caller of `Run`, cancellation = `<-ctx.Done()`, termination = ctx done OR top-frame `recover`. Boot retry NOT implemented here — `ErrBootTimeout` declared so SDD-23 can produce it (R-010); smoke tests assert no internal retry + no Discord prompt during refill failure (FR-021-19). DM rate limit NOT implemented here — orchestrator's `refill` callback wraps SDD-11's BotApprover; `ErrRateLimited` from BotApprover surfaces as Refresher WARN log + advance, never state transition (chunk-doc HOW). Coverage ≥95% (SC-021-10) verified by `go test -race -cover ./internal/supervise/ -run "Refill|Refresh|Grace"`. Lifecycle Scenarios 3, 7, 8, 9, 11 each have a passing test (SC-021-1). **Anti-contracts**: NEVER `string(decryptedBytes)` anywhere (Constitution X / FR-021-15 / SC-021-8 — JWT bearer-header `string(...)` site is the **only** permitted materialization, scoped to `Snapshot.Token.Use(func(b []byte){})` closure); NEVER spawn a goroutine in `NewGrace` or any constructor (Constitution IX); NEVER propagate `refill` callback error from `Refresher.Run` (FR-021-11a); NEVER destroy the value passed to `Grace.Set` when disabled (R-009 — caller retains ownership); NEVER retry inside `Refill` (boot retry is SDD-23's job, R-010); NEVER return `ErrJTIUnknown` for any 4xx other than `401 + {"error":"unknown_jti"}`; NEVER add `ErrTransient` sentinel (R-006 final — Go stdlib error types provide the typed-distinguishability FR-021-4 demands); NEVER add `RunSweeper`-style exported method on `Grace` (R-008 final — lazy-evict suffices); NEVER add `init()`; NEVER add package-level mutable state; NEVER alter any SDD-18 / SDD-19 / SDD-20 symbol; NEVER write to the audit log directly (orchestrator's job, R-012). **Zero new direct dependencies** — stdlib + existing `internal/vault/securebytes` + existing `internal/transport/ecies` only. Constitution scope: IV (4h grace cap; lifecycle integrity — chunk emits typed errors only, SDD-23 drives transitions), V (Refresher WARN on rate-limit drop = operator-visible loud failure), VIII (TDD-mandatory; 26 tests authored before implementation; race-clean; ≥95% coverage), IX (no init, no globals, single Refresher tick goroutine with owner+ctx+termination+recover, sentinel-class `var Err…`, errors wrap with `%w`), X (`*SecureBytes` flow only — no `string(decryptedBytes)`; type-driven redaction via `LogValue() → "[redacted]"`; marker-byte capture tests assert no leakage). AC-10 primary.
<!-- SPECKIT END -->
