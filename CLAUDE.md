# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/013-server-handlers-and-audit/plan.md](specs/013-server-handlers-and-audit/plan.md) — SDD-13 `internal/server` `GET /h/<prefix>/s/<name>` + `POST /h/<prefix>/revoke` + `GET /h/<prefix>/hz` handlers AND new `internal/audit` package (hash-chained, ECDSA-signed event log + best-effort Discord mirror). `/s`: Bearer JWT → `token.Validate` (SDD-07) → `vault.Store.Get` → `ECIES.Encrypt` (SDD-09) → octet-stream body. `/revoke`: signed body (jti+nonce+timestamp+fingerprint) → `sign.CanonicalJSON+Verify`+NonceCache+IsFreshTimestamp → `token.Store.Revoke`; idempotent re-revoke returns identical 200 body with audit-only distinction. `/hz`: no auth (Constitution VI), reports `{status,uptime,secrets_count,active_tokens,discord_connected,config_valid,vault_loaded,clock_in_sync}`, never emits audit events. `internal/audit`: locked surface `Event{Seq,Time,Action,Data,PrevHash,Hash,Signature}` + `Writer{Append,Run}` + `DiscordMirror` + `ErrAuditChainBroken` + `Verify`. Single goroutine owns the file handle and the chain; producer-side `Append` is rendezvous-synchronous (FR-033) and blocks under contention (FR-031); mirror is a separate non-blocking goroutine. Hash = `SHA-256(prevHash || CanonicalJSON(Event-without-Hash-or-Signature))`; signature is ECDSA over the hash bytes. Genesis = `sha256("hush.audit.chain.v1.genesis")`. Adds `Deps.DiscordHealth func() bool`, `BotApprover.Connected()`, `vault.Store.Names()`, `token.Store.ActiveCount()`, `ErrSecretMissing`, `chassisAuditAdapter`. Coverage targets: handlers ≥ 95%, audit = 100%. Sentinel-leak tests use `SECRET_SHOULD_NEVER_APPEAR_13`. AC-1/2/4/7.
<!-- SPECKIT END -->
