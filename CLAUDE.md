# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/017-cli-secret/plan.md](specs/017-cli-secret/plan.md) — SDD-17 `hush secret` add/remove/list/rotate vault management (TTY-only writes; SIGHUP reload). New files: `internal/cli/secret.go` + `internal/cli/secret_test.go`; mounted via `root.AddCommand(newSecretCmd())` in `Execute`. **No new exported symbols** in `internal/cli` (cobra command tree IS the contract). No new exit-code constants — verb-internal sentinels (`errInvalidSecretName`, `errSecretValueMismatch`, `errConfirmationMismatch`, `errSecretExists`) wrap existing classifiers in `mapErr`. Universal stdin-TTY gate: every verb (incl. `list`) checks `term.IsTerminal(int(os.Stdin.Fd()))` BEFORE any vault I/O / passphrase prompt; non-TTY → `ExitInputErr` with locked stderr msg "hush: secret: this command requires an interactive TTY (rogue-process defence)". Structural absence of `--value`/`--secret`/`--password` flags (cobra rejects with "unknown flag"); SC-007 + FR-003. Name validation `^[A-Z_][A-Z0-9_]*$` length 1–64 runs BEFORE vault open (FR-017). Passphrase resolution reuses `readPassphraseTTY` from `init.go` (NOT serve's pipe-fallback `resolvePassphrase`). Salt via `readVaultSalt`; master seed via `keys.DeriveMasterSeed`; AES key via `keys.DeriveVaultEncKey`. `add`: TTY → name-validate → passphrase → value (no echo) → confirm-value (byte-equal compare; mismatch → `errSecretValueMismatch` → `ExitInputErr`) → optional description (echoing line) → load → exists-check (`errSecretExists` → `ExitErr` with msg directing operator to `hush secret rotate`) → append → `vault.Save` (atomic per SDD-03). `remove`: TTY → name-validate → passphrase → load → not-found (`vault.ErrSecretNotFound` → `ExitNotFound`) → typed-name confirmation prompt (echoing; byte-equal compare; mismatch → `errConfirmationMismatch` → `ExitInputErr`) → filter → save. `list`: stdin-TTY-gate → passphrase → load → enumerate via `store.Names()` → `store.Get(name)` → IMMEDIATELY `Destroy()` value `*SecureBytes` (renderer NEVER touches `Secret.Value`) → sort ascending by Name → choose by `term.IsTerminal(os.Stdout)`: TTY → `NAME — description\n` (em-dash U+2014; empty desc → `NAME\n`); pipe → `json.NewEncoder(stdout).Encode([]listEntry{Name,Description})` (locked field set; populated → `[{"name":"…","description":"…"},…]\n`; empty → `[]\n`). Empty-vault TTY hint → stderr `(vault is empty)`. `rotate`: TTY → passphrase → load → re-Save (SDD-03 mints fresh nonce+salt; ciphertext bytes change, plaintext set preserved; FR-009/SC-003) → read `<state_dir>/hush.pid`: present+`syscall.Kill(pid,0)==nil` → `syscall.Kill(pid,SIGHUP)` + INFO stderr "hush: secret: signalled running server (pid=%d)"; absent/`ESRCH`/`EPERM`/unparseable → WARN stderr "hush: secret: no running server signalled (...)" → `ExitOK` in EVERY branch (FR-011/SC-005). PID-file path literal `"hush.pid"` inlined as `pidFilename` constant; `serve` does NOT yet write it (future SDD chunk) — `pidAbsent` is the dominant production branch today. Defer chain (LIFO): Destroy each secret SB → Destroy vault key SB → Destroy passphrase SB. Audit: slog INFO on success (`secret_added`/`secret_removed`/`vault_rotated`); slog WARN on security-relevant failure (`secret_tty_refused`/`secret_passphrase_failed`/`secret_confirmation_mismatch`); `list` success NOT audited (read-only); routine input-validation refusals (bad name, missing arg, unknown flag) NOT audited (FR-015). NO record carries secret/passphrase/token bytes. **Anti-contracts**: NEVER declare `--value`/`--secret`/`--password` flag; NEVER read passphrase from a pipe; NEVER print `Secret.Value` to ANY stream; NEVER signal a PID we don't own; NEVER fail rotate on missing PID; NEVER touch `exit_codes.go`. Coverage target 85% on `internal/cli` (secret portion); sentinel = `SECRET_SHOULD_NEVER_APPEAR_17`. Constitution scope: VII (cobra noun-verb, exit codes, TTY/pipe rendering), X (SecureBytes, no value in logs/errors), Security Requirements (TTY enforcement, name validation, file mode 0600 inherited, no hardcoded secrets). AC-1, AC-2 (write half of vault round-trip). No new direct dependencies — reuses SDD-01 `keys`, SDD-02 `securebytes`, SDD-03 `vault`, SDD-06 `config`, plus stdlib `regexp`/`syscall`/`encoding/json`/`sort`. Tests use pty fixtures + `testutil.NewTestVault`; SIGHUP delivery proven by helper child writing `<state_dir>/hush.pid`. Validate gates pass on darwin AND linux.
<!-- SPECKIT END -->
