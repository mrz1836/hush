# CLAUDE.md

See [.github/CLAUDE.md](.github/CLAUDE.md) for the canonical agent guide.

<!-- SPECKIT START -->
Active feature plan: [specs/019-supervise-state/plan.md](specs/019-supervise-state/plan.md) — SDD-19 `internal/supervise` state machine + snapshot store. **Seeds `package supervise`** (alongside the pre-existing SDD-18 `config/` subpackage) with three files: `doc.go`, `state.go` (locked surface: `State`/`Event` enums, `Store` with `sync.RWMutex`, `Snapshot` value-type, `Clock` interface, `ErrInvalidTransition` sentinel, package-internal `transitions map[State]map[Event]State` + `reasons map[Event]string`), `state_test.go` (15 tests: T-01..T-15 per `specs/019-supervise-state/contracts/test-catalogue.md`). Closed state vocabulary = exactly 5 (`fetching/running/awaiting-approval/grace-restart/stopped`); closed event vocabulary = exactly 15 (transcribed from Scenarios 2-15 of `docs/LIFECYCLE-SCENARIOS.md` per FR-019-21). Initial state `StateFetching` (FR-019-16); `StateStopped` is terminal but `EventStopRequested` is idempotent from any state including `stopped` (FR-019-17, R-010). Recovery from `awaiting-approval` is **two-step** — `EventApprovalGranted` → `fetching` then `EventFetchOK` → `running` (Clarification 2 / FR-019-19). Grace re-entry permitted within session — `running ↔ grace-restart` repeats; upper bound is upstream `cache_grace_ttl` timer firing `EventGraceExpired` to drive `grace-restart → awaiting-approval` (Clarification 3 / FR-019-18). `Reason` field is **store-derived** from the locked event-to-phrase map (Clarification 1 / FR-019-7); callers MUST NOT supply reason text — `Transition(ctx, event)` carries no reason argument. `LastTransitionAt` from injected `Clock` interface at commit time (Clarification 4 / FR-019-20) — single-method interface defined at the consumer per Constitution IX; `nil` clock at `NewStore` panics (startup-wiring exemption). Token (`*securebytes.SecureBytes` — locked at SDD-02) renders `[redacted]` via `LogValue() slog.Value`; bytes are borrow-only via `Use(fn)` (FR-019-9..11, Constitution X); released tokens `Destroy()`'d post-swap (R-007). `Snapshot` returns by-value (FR-019-8); pointer-shared `*SecureBytes` is safe by SDD-02 contract. State-table is package-level `var transitions = map[State]map[Event]State{...}` populated as a single literal — read-only post-init, sentinel-class equivalent to `var Err... = errors.New(...)` per Constitution IX (R-002). 5×15=75 cells; **19 legal cells** require positive tests (`TestStore_LegalTransitions`); **56 illegal cells** require negative tests asserting `errors.Is(err, ErrInvalidTransition)` + unchanged store (`TestStore_IllegalTransitionErr`) per SC-019-1. Wrap form: `fmt.Errorf("supervise: %w (state=%s event=%s)", ErrInvalidTransition, current, event)` — both names reach the operator log (FR-019-15, SC-019-7). Concurrency: `sync.RWMutex` (R-001) — `Transition` write-locks, `Snapshot` read-locks; commit-style transitions (FR-019-14); race-detector clean across 100-iteration concurrent test (SC-019-2). **No fuzz target added** at this layer — events arrive as typed Go constants, not parsed strings; fuzz #5 (TOML) is owned by SDD-18 (R-008). **Token-write seam deferred to SDD-21** — public surface is `NewStore(ctx, Clock) *Store`, `Transition(ctx, Event) error`, `Snapshot() Snapshot`; `SetToken` is NOT exported in this chunk (R-009). **Anti-contracts**: NEVER export `SetToken`/`State()`/`ChildPID()` per-field accessors — readers go through `Snapshot()`; NEVER swap the clock via a package-level `var nowFunc = time.Now` (use the `Clock` interface, R-004); NEVER spawn a goroutine (FR-019-12); NEVER perform any I/O (no `os`/`net`/filesystem/socket/Discord/audit calls — FR-019-13); NEVER model Scenario 8 (JWT-only refresh) as a state-machine event — child posture toward parent is unchanged; NEVER add a per-session grace-restart re-entry counter; NEVER add a composite `awaiting-approval → running` event; NEVER introduce `internal/testutil` helpers — fakes inline in `state_test.go` per SDD-18 pattern (R-011); NEVER add or rename a `State`/`Event` constant outside a SPEC amendment; NEVER alter any SDD-18 symbol or the `config/` subpackage. Coverage target ≥95% on `internal/supervise/state.go` (Constitution VIII High band). Constitution scope: IV (TTL via state model — child exit never reaches `stopped`), V (status socket sees `Snapshot`, exit-78 = first-class state edge, validator failure = first-class state edge), VIII (TDD-mandatory; 15 tests T-01..T-15 authored before impl; race-clean), IX (no `init`, no goroutines, single-method interface at consumer, errors wrapped with `%w`, modules-only, CGO-disabled), X (Token redacts via SDD-02 contract; no secret bytes in errors). AC-10 primary. **Zero new direct dependencies** (stdlib + internal `securebytes` only). Tests use inline fakes + table-driven harnesses; no new external test helpers.
<!-- SPECKIT END -->
